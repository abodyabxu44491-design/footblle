# 📡 خطة تطوير البث — «مثل القنوات الكبيرة» بلا مشاكل

> تشخيص من الكود الفعلي (مو تخمين) + حلول عملية مرتّبة بالأولوية. البداية بإصلاح ما هو مكسور، ثم الترقية لمستوى القنوات.

---

## أولاً: المشاكل الحقيقية (شخّصتها من كودك)

### 🔴 م1) صدى الصوت + تشويه الجمهور — السبب الجذري

في `broadcaster.html` سطر 819، الميكروفون يُلتقط هكذا:

```js
audio:{ echoCancellation:true, noiseSuppression:true, autoGainControl:true, channelCount:1 }
```

**هذه إعدادات مكالمة صوتية (Zoom/واتساب)، وهي كارثة لبث رياضي:**
- `echoCancellation` مصمّم لإلغاء صوت السماعة الراجع في المكالمات — لكنه في الملعب "يبلع" هتاف الجمهور ويسبب صدى متقطّعاً وتشويشاً حين يتغيّر مستوى الصوت فجأة.
- `noiseSuppression` يعتبر صوت الجمهور والصفارة "ضوضاء" فيكتمها → صوت مكتوم ميت لا يشبه أجواء الملعب.
- `autoGainControl` يرفع ويخفض المستوى تلقائياً فيسبب "تنفّس" الصوت (يعلو ويهبط بشكل مزعج).

**الصدى الحقيقي غالباً ينشأ** حين يكون المذيع ومشاهد في نفس المكان، أو حين يفتح المذيع صفحة الجمهور على نفس الجهاز — فيدخل الصوت ويخرج ويرجع.

**✅ الحل (استبدل السطر 819):**

```js
audio:{
  echoCancellation:false,   // بث رياضي، لا مكالمة — نريد صوت الملعب كاملاً
  noiseSuppression:false,   // لا نكتم هتاف الجمهور
  autoGainControl:false,    // مستوى ثابت بلا "تنفّس"
  channelCount:2,           // ستيريو (أجواء أوسع)
  sampleRate:48000
}
```

ثم أضف **خيار للمذيع** (زر «وضع الصوت»): «أجواء الملعب» (الأعلى) مقابل «تعليق صوتي» (يُعيد المعالجة `echoCancellation:true` لو المذيع يعلّق بمايك قريب من فمه). هكذا كل موقف له إعداده الصحيح.

**وامنع الصدى نهائياً عند المشاهد** (viewer.js سطر ~4354): الفيديو أصلاً يبدأ `muted` — جيد. لكن أضف عند المذيع تحذيراً واضحاً: «لا تفتح صفحة المشاهدة على نفس الجهاز أثناء البث» (هذا أشهر سبب صدى).

---

### 🔴 م2) شريط الوقت والنتيجة — وميض وثقل

في `viewer.js` الدالة `_psUpdateScorebar` (سطر 4491) تُعيد بناء **كل** الـ `innerHTML` (بما فيه وسم `<img>` للشعارات) **كل ثانية** عبر `_psScoreTick`. النتيجة:
- وميض مرئي كل ثانية.
- إعادة تحميل الشعارات من الشبكة بشكل متكرر → ثقل ووميض صور.
- الشريط صغير (13px)، ثابت أعلى-يسار، وتصميمه لا يشبه القنوات.

**✅ الحل — افصل «البناء» عن «التحديث»:**

ابنِ الشريط **مرة واحدة** (الشعارات والأسماء)، وحدّث **فقط النص المتغيّر** (النتيجة + الوقت) كل ثانية:

```js
// يُبنى مرة واحدة عند ظهور البث
function _psBuildScorebar(m){
  const bar = document.getElementById('ps-scorebar-'+m.id);
  if(!bar) return;
  const ht = (window.teams||[]).find(t=>t.id===m.homeId)||{name:m.homeName,logo:m.homeLogo};
  const at = (window.teams||[]).find(t=>t.id===m.awayId)||{name:m.awayName,logo:m.awayLogo};
  bar.innerHTML =
    `<div class="ps-side home">${_psLogo(ht)}<span class="ps-tm">${ht.name}</span></div>`+
    `<div class="ps-sc"><span id="ps-hs-${m.id}">0</span><span class="ps-dot"></span><span id="ps-as-${m.id}">0</span></div>`+
    `<div class="ps-side away"><span class="ps-tm">${at.name}</span>${_psLogo(at)}</div>`+
    `<div class="ps-ck" id="ps-ck-${m.id}"></div>`;
  bar._built = true;
}

// يُستدعى كل ثانية — يغيّر النص فقط، بلا إعادة بناء
function _psUpdateScorebar(m){
  const bar = document.getElementById('ps-scorebar-'+m.id);
  if(!bar) return;
  if(!bar._built) _psBuildScorebar(m);
  const d = m.liveData||{};
  const hs = d.homeScore ?? m.homeScore ?? 0;
  const as = d.awayScore ?? m.awayScore ?? 0;
  const ckRaw = (typeof _clock==='function')? _clock(d):'';
  const ck = (ckRaw && ckRaw!=='--')? ckRaw:'';
  const H=document.getElementById('ps-hs-'+m.id), A=document.getElementById('ps-as-'+m.id), C=document.getElementById('ps-ck-'+m.id);
  if(H && H.textContent!=String(hs)){ H.textContent=hs; _psFlash(H); }  // وميض ذهبي عند الهدف فقط
  if(A && A.textContent!=String(as)){ A.textContent=as; _psFlash(A); }
  if(C) C.textContent = ck;
  bar.style.display = '';
}
```

فائدة إضافية: تقدر تعمل **وميض/أنيميشن ذهبي عند الهدف فقط** (لأنك الآن تعرف متى تغيّرت النتيجة) — تماماً مثل القنوات.

---

### 🟠 م3) الوقت أحياناً «--» أو متأخّر

الوقت يُحسب من `_clock(liveData)` المعتمد على `half1StartedAt` وساعة الخادم. لو تأخّر تحميل `liveData` لحظة الفتح، يظهر «--». هذا مغطّى جزئياً (v191) لكن يبقى:
- **✅ الحل:** خزّن آخر وقت معروف محلياً واعرضه فوراً ريثما يصل الجديد (بدل «--»)، واعرض مؤشّر «الوقت من الإدارة» صغيراً حتى يفهم المشاهد أن المصدر هو المنظّم.

---

## ثانياً: ترقية لمستوى القنوات الكبيرة (بالترتيب)

### الطبقة 1 — تصميم الشريط الاحترافي (سريع، أثر كبير)

اجعل الشريط يشبه bein/قنوات الدوري:
- **الموضع:** أسفل-وسط أو أعلى-وسط بعرض مناسب (لا زاوية صغيرة).
- **الهيكل:** [شعار+اسم مختصر الفريق] [النتيجة كبيرة] [شعار+اسم] ... ثم كبسولة منفصلة للوقت بلون ذهبي.
- **الاسم المختصر:** 3 أحرف للفريق (مثل القنوات: RMA / BAR) مع الشعار.
- **شارة الشوط:** «ش1 / ش2 / و.إ / ركلات» بجانب الوقت.
- **هوية المنصة:** خلفية زجاجية داكنة + إطار ذهبي `#C9A02B` ليتّسق مع باقي المنصة.
- **شعار البطولة/الراعي** في ركن الشريط (قيمة تسويقية للراعي).

### الطبقة 2 — جودة صوت وصورة القنوات

- **صوت:** بعد إصلاح م1، ارفع bitrate الصوت لـ 128kbps ستيريو (الكود حالياً لا يضبط صوتاً منفصلاً — أضف encoding للصوت).
- **صورة:** الكود يبدأ 720p ثم يخفض تكيّفياً — جيد. أضف خيار **1080p للبطولات الكبيرة** (عند استخدام HLS لا P2P، حيث لا يحدّه رفع الجوال).
- **حقن اللوجو (Watermark):** ارسم شعار البطولة على الفيديو نفسه (canvas قبل الإرسال) فيظهر حتى لو صُوّرت الشاشة — مثل شعار القناة.

### الطبقة 3 — مزايا القنوات المتقدّمة

هذه موجودة جزئياً أو ناقصة:

| الميزة | الحالة | الاقتراح |
|---|---|---|
| الإعادة اللحظية (Replay) | ✅ موجودة (v201) | حسّنها: إعادة بطيئة + اختيار مدة (6/12/20 ثانية) |
| شريط النتيجة الحيّ | ✅ موجود | أصلح الوميض (م2) + تصميم القنوات |
| التعليق النصي الحيّ | ⚠️ أُزيل (v190) | أعده كطبقة اختيارية للمذيع/معلّق منفصل |
| **الجرافيك (Lower-third)** | ❌ ناقص | بطاقة تظهر أسفل الشاشة: «⚽ هدف — اسم اللاعب 34'» تلقائياً من أحداث الإدارة |
| **الإحصائيات الحيّة على البث** | ❌ ناقص | استحواذ/تسديدات كشريط شفّاف عند الطلب |
| **تعدّد الكاميرات** | ❌ ناقص | السماح بأكثر من مذيع لنفس المباراة والتبديل بينهم |
| **التقاط الشاشة/OBS** | ⚠️ جزئي (HLS) | دليل واضح لربط OBS للبطولات الكبيرة |
| **مؤشر جودة الاتصال** | ✅ موجود | أضف رقم bitrate/ping الفعلي للمذيع |

### الطبقة 4 — الموثوقية (أهم من الكماليات)

1. **فعّل TURN فعلياً:** الكود جاهز (`TURN_CONFIG` معلّق سطر 550). بدونه ~10% من المشاهدين لا يتصلون إطلاقاً. سجّل مجاناً في metered.ca وضع المفتاح. **هذا أهم إصلاح موثوقية.**
2. **إعادة الاتصال:** موجودة (v193، 5 محاولات) — جيد.
3. **حدّ P2P الفيزيائي:** 15–50 مشاهد حسب رفع المذيع. للأكبر: **وجّه صاحب البطولة لـ HLS** (موجود). لا تعِد المستحيل — كن صريحاً في الواجهة عن الحدّ.
4. **SFU مستقبلاً:** لو أردت مئات المشاهدين بجودة ثابتة بلا اعتماد على صاحب البطولة، الحل النهائي خادم SFU خفيف (mediasoup/LiveKit). بنية الإشارة الحالية جاهزة له. **لكن له تكلفة عليك** — قرار تجاري.

---

## ثالثاً: خطة تنفيذ مرتّبة (ابدأ من فوق)

**المرحلة 1 — إصلاحات فورية (يوم واحد):**
1. إصلاح إعدادات الصوت (م1) — يحلّ الصدى/التشويه.
2. فصل بناء/تحديث الشريط (م2) — يحلّ الوميض والثقل.
3. تفعيل TURN — يحلّ فشل اتصال 10% من المشاهدين.

**المرحلة 2 — تصميم القنوات (أيام):**
4. إعادة تصميم الشريط (موضع/حجم/هوية ذهبية/اسم مختصر/شارة شوط/شعار راعٍ).
5. وميض ذهبي عند الهدف + صوت 128kbps ستيريو.

**المرحلة 3 — مزايا التميّز (أسبوع+):**
6. Lower-third تلقائي للأحداث (هدف/بطاقة باسم اللاعب من الإدارة).
7. تحسين الإعادة (بطيئة + مدة قابلة للاختيار).
8. حقن شعار البطولة على الفيديو (watermark).

**المرحلة 4 — قرار تجاري:**
9. تقييم SFU للأعداد الكبيرة (تكلفة مقابل قيمة).

---

## الخلاصة الصريحة

مشاكلك حقيقية وسببها واضح في الكود:
- **الصدى** = إعدادات مكالمة صوتية خاطئة على بث رياضي → إصلاح سطر واحد.
- **وميض الشريط** = إعادة بناء DOM كل ثانية → فصل البناء عن التحديث.
- **عدم مناسبته** = تصميم لا يتّسق مع هوية المنصة + TURN غير مفعّل.

الأساس الهندسي عندك **ممتاز** (TimerCore، فصل الأدوار، تعدّد الطبقات) — لا يحتاج إعادة بناء، بل **ضبط**. طبّق المرحلة 1 أولاً وستختفي 80% من شكواك فوراً، ثم المرحلة 2 لمظهر القنوات. لا تقفز للمزايا قبل إصلاح الصوت والشريط وTURN.
