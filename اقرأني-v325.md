# v325 — رمز الهدف يظهر كرةً لا نقطة صفراء

## السبب

أيقونات المنصة مرسومة **بالخطوط**، لا بالمساحات:

```html
<svg fill="none" stroke="currentColor" stroke-width="1.9">
  <circle cx="12" cy="12" r="9"/>   ← محيط الكرة
  <path d="M12 7.5l3.3 2.4…"/>       ← الخماسي الداخلي
  <path d="M12 3v4.5M4.6 9.8…"/>     ← الخطوط الخارجية
</svg>
```

وحين حذفتُ الخلفية الدائرية في النسخة السابقة، لوّنتُ الرمز بقاعدة:

```css
.vt-dot-goal svg, .vt-dot-goal path, .vt-dot-goal circle { fill: var(--gold) }
```

فامتلأت **دائرة الكرة الخارجية** ذهباً وابتلعت تفاصيلها الداخلية — فظهر
الهدف **نقطة صفراء صمّاء** لا كرة.

## الإصلاح

التلوين الصحيح لأيقونات الخطوط يكون على **`color` وحده**:
`stroke="currentColor"` يتبعه تلقائياً، ويبقى الداخل مفرَّغاً فتظهر خطوط
الكرة.

```css
.vt-dot{ color: var(--t2) }
.vt-dot-goal{ color: var(--gold) }
.vt-dot-goal.vt-dot-own{ color: var(--red) }
.vt-dot-inj{ color: var(--orange) }
.vt-dot-var{ color: var(--blue) }
.vt-dot-pkmiss{ color: #9B59B6 }
.vt-dot svg{ fill:none; stroke:currentColor }
```

وسرى ذلك على كل رموز الخطوط: الكرة · الإصابة · VAR · ركلة الجزاء.
أما البطاقات وسهما التبديل فمرسومة بالمساحات أصلاً ولم تتأثّر.

## الاختبارات

محاكاة بمَتن الأيقونة الحقيقي: `fill="none"` مثبت، وعنصرا الكرة
(`circle` + `path`) قائمان، و`fill` المحسوب شفاف — **أي خطوط لا كتلة**.
ولا قاعدة تملأ `circle` بعد اليوم.

وإعادة تشغيل **ثماني سلاسل** — صفر أخطاء.

## بعد الرفع

`batolat-v325`. امسح بيانات الموقع مرة واحدة.
