# حزمة بطولات v219 — الحزمة الكاملة بآخر تحديث

هذه **الحزمة كاملة وجاهزة للرفع** (65 ملفاً) — تحتوي كل ملفات مشروعك مع كل
التعديلات مدمجة. ارفعها كما هي.

## ✅ رقم الـ Service Worker مرفوع بالفعل

`sw.js` صار `batolat-v219` (كان `batolat-v117` متأخراً أكثر من 100 إصدار).
**لم تعد بحاجة لتعديله يدوياً** — لكن تذكّر رفعه في كل إصدار قادم، وإلا قدّم
الـ Service Worker للجمهور كوداً قديماً مكيّشاً ولم تظهر أي تحديثات.

---

## 📦 محتوى هذه الحزمة (ثلاث دفعات تراكمية)

### الدفعة الثالثة — صور اللاعبين (هذا التحديث)
التفاصيل الكاملة في **`PLAYER-PHOTOS-SETUP.md`**.

| العطل | السبب الجذري | الحل |
|---|---|---|
| الصورة لا تتغيّر | معرّف ثابت + الرفع غير الموقّع لا يسمح بالاستبدال، فيُرجع Cloudinary الأصل القديم بنفس الرابط | معرّف فريد لكل رفعة → رابط جديد دائماً |
| الصورة لا تُحذف | **زر الحذف غير موجود في الواجهة أصلاً**، والدالة تمسح الرابط فقط والملف يبقى | زر 🚫 + حذف حقيقي عبر `delete_by_token` + سلّة للمتعذّر |
| اختلاط الصور بين المتشابهين | الدالة تمسح **كل الفرق** وترجع صورة أي متشابه في الاسم | الهوية تحسم → الاسم+الرقم → الاسم الفريد فقط → لا عبور فرق إطلاقاً |

**⚙️ إعداد مطلوب مرّة واحدة:** فعّل «Return delete token» في preset الـ Cloudinary
(`wvebrqwq`). بدونه كل شيء يعمل عدا الحذف الفوري.

**الاختبارات:** 18 اختبار وحدة لمطابقة الهوية + 11 اختبار للرفع/الحذف ضد محاكاة
لسلوك Cloudinary الحقيقي — **29 اختباراً، صفر فشل**.

---

### الدفعة الثانية — الشجرة وبطاقة المشاركة
- **إعادة بناء شجرة الإقصاء**: التصميم القديم كان يرصّ خانات الدور في عمود واحد،
  فخط الاتصال ينزل **مخترقاً البطاقات تحته** والبطاقات تحجبه — لذلك بدت الخطوط
  مقطّعة وعشوائية. صارت أعمدة أفقية RTL بمحاذاة `space-around`، فكل مباراة تقع
  تلقائياً في منتصف مغذّيتيها مهما كبرت الشجرة.
- **كل البطاقات بمقاس واحد** (حتى غير المحسومة) · شعارات **26px** · خطوط مرفقية
  من الحواف فقط · زر «عرض الشجرة كاملة» · نص «بانتظار المتأهل» بدل TBD.
- **بطاقة المشاركة (canvas)** أُعيد بناؤها بنفس القواعد الرياضية فتطابق الشاشة.
- **قياسات دور 32:** 31 بطاقة بمقاس واحد (178×74)، 30 خط اتصال، صفر أخطاء.

### الدفعة الأولى — نظام الصناعة
- خانة الصانع في **الإدخال السريع** (كانت في النظام المهجور فقط).
- سطر «👟 صناعة: فلان» تحت الهدّاف في الخط الزمني.
- شارة صناعة على اللاعب في التشكيلة — أُصلح خطأ منطقي كان يجعلها **مستحيلة**
  (الدالة تخرج من الحدث قبل فحص الصانع، وصانع الهدف ليس مسجّله).
- **أيقونة `boots` كانت مفقودة** من مكتبة الأيقونات — كانت تكسر «صانع الألعاب»
  ونافذة اللاعب عندك من قبل.

---

## ⚠️ ملاحظة أمنية لم تُعالَج بعد (من فحصي الأول)

`firestore.rules` **لا تحتوي أي قاعدة لمجموعة `admins`**، بينما `superadmin.js`
يتحقق من هوية السوبر أدمن بقراءة `admins/{uid}`. فتقع تحت قاعدة الرفض الشاملة:
القراءة تُرفض → لا يدخل السوبر أدمن أبداً. وحتى لو دخل، إنشاء البطولات وكتابة
`leagueAdmins` و`subscriptions` و`settings/platform` كلها مرفوضة.

يعني أحد أمرين: إمّا القواعد غير منشورة وقاعدة بياناتك مفتوحة (ثغرة كاملة)، أو
منشورة ولوحة السوبر أدمن معطّلة. **قل لي وأعالجها** — تحتاج إضافة دالة
`isSuperAdmin()` وقواعد للمجموعات الأربع.
