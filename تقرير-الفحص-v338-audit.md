# تقرير فحص منصة Footballa — النسخة v338

**تاريخ الفحص:** 15 سبتمبر 2026
**نطاق الفحص:** المنصة كاملة — Super Admin · لوحة إدارة الدوري · صفحة الجمهور · الاشتراك · البث · التسليم · قواعد Firebase

---

## ملخّص تنفيذي

| | العدد |
|---|---|
| ملفات JS مفحوصة | 35 (صفر أخطاء صياغة) |
| صفحات HTML مفحوصة | 7 |
| مشاكل **حرجة** | 6 |
| مشاكل **عالية** | 5 |
| مشاكل **متوسطة** | 6 |
| أُصلح في هذه الجولة | 11 |

**أهم نتيجة:** ملف `firestore.rules` الموجود في المشروع **يشلّ لوحة السوبر أدمن بالكامل** إذا نُشر. لا يستطيع السوبر أدمن الدخول، ولا إنشاء بطولة، ولا منظّم، ولا اشتراك. وإلى جانبه ثغرة تصعيد صلاحيات تتيح لأي شخص على الإنترنت أن يصير سوبر أدمن.

> **ملاحظة مهمة:** إن كانت لوحتك تعمل اليوم، فالقواعد المنشورة على Firebase ليست هذه النسخة. الملف في المشروع لم يُنشر بعد — وهذا خطر قائم: أي `firebase deploy --only firestore:rules` يُسقط المنصة فورًا.

---

## بنية المنصة كما فُهمت

تطبيق ويب خالص بلا build system. النواة `admin.js` (1.1 ميجابايت / 19,300 سطر)، وحولها ملفات «ترقيع» تغلّف دوالّ النواة عبر `window.X = wrapper(orig)`.

**آلية التحميل — تحققت منها:** `admin.js` مُحمّل كـ `type="module"` فينفّذ **بعد** كل السكربتات العادية. لذلك تستخدم ملفات الترقيع حلقة `ready()` بـ `setTimeout` تنتظر ظهور دوالّ النواة قبل التغليف. التصميم سليم لكنه هشّ: لو تأخّرت النواة أكثر من عدد المحاولات، تموت الميزة بصمت.

**دوالّ مُغلَّفة في أكثر من ملف** (سلسلة التغليف تعمل، لكنها نقطة هشاشة):
`lpEndMatch` (3 ملفات) · `openLivePage` (4) · `showPage` (8) · `renderAll` (3)

**المجموعات في Firestore:**
`leagues` · `leagueAdmins` · `admins` · `settings` · `subscriptions` · `subRequests` · `handoverLinks` · `liveStreams`
وتحت كل بطولة: `matches` · `teams/{id}/roster` · `groups` · `knockoutRounds` · `config` · `notifTokens` · `photoTrash` · `scorers` · `live`

---

# القسم الأول: المشاكل الحرجة

## 🔴 ح-1 — لوحة السوبر أدمن معطّلة بالكامل

التعليقات في ملف القواعد تقول «السوبر أدمن / Console فقط» — لكن اللوحة **ليست** Firebase Console، بل صفحة ويب تستعمل Web SDK، وقواعد الأمان تسري عليها بالكامل.

| العملية | الموضع | القاعدة | النتيجة |
|---|---|---|---|
| فحص صلاحية الدخول | `superadmin.js:121` | مجموعة `admins` **غائبة** | **الدخول مستحيل** |
| إنشاء بطولة | `superadmin.js:352, 947` | `create: if false` | فشل |
| حذف بطولة | `superadmin.js:558` | `delete: if false` | فشل |
| إنشاء منظّم | `superadmin.js:954` | `leagueAdmins write: if false` | فشل |
| حذف منظّم | `superadmin.js:806` | `leagueAdmins write: if false` | فشل |
| إنشاء اشتراك | `superadmin.js:966, 1028` | `subscriptions write: if false` | فشل |
| تجديد / إلغاء اشتراك | `superadmin.js:693, 716` | `subscriptions write: if false` | فشل |
| حفظ إعدادات المنصة | `superadmin.js:1053` | مجموعة `settings` **غائبة** | فشل |

**الأسوأ — رسالة مضلّلة:** `getDoc(doc(db,'admins',uid))` يُرفض بـ `permission-denied`، ويلتقطه `catch` فيعرض «خطأ في تسجيل الدخول» — أي يوحي بخطأ في كلمة المرور بينما السبب قاعدة ناقصة. وكذلك `onAuthStateChanged` يرمي رفضًا غير ملتقَط.

**✅ أُصلح** — أُضيفت قواعد `admins` و`settings`، ومُنح `isSuperAdmin()` كل الصلاحيات التي تحتاجها اللوحة فعليًا.

---

## 🔴 ح-2 — تصعيد صلاحيات إلى سوبر أدمن

```
isSuperAdmin() = signedIn() && !hasAdminRec()
```

الترجمة: «أي مستخدم مسجّل دخول وليس منظّم بطولة = سوبر أدمن».

**سيناريو الاستغلال:** مزوّد Email/Password مفعّل (لأن `createUserWithEmailAndPassword` يُستدعى من المتصفح)، و`firebaseConfig` منشور في مصدر الصفحة — ولا مفرّ من ذلك، فهو تصميم Firebase. فأي شخص:

1. ينسخ `firebaseConfig` من مصدر أي صفحة
2. ينفّذ `createUserWithEmailAndPassword` من طرفية المتصفح
3. يحصل على حساب بلا سجلّ `leagueAdmins` → **يصير سوبر أدمن فورًا**
4. يقرأ كل `subRequests` — **بما فيها كلمات مرور عملائك نصًّا صريحًا**
5. ينشئ ويحذف `handoverLinks`

**✅ أُصلح** — `isSuperAdmin()` صار يفحص `admins/{uid}.role == 'superadmin'`، وهو نفس ما تفحصه `superadmin.js:121` — فتطابقت الواجهة مع الخادم.

---

## 🔴 ح-3 — تجاوز صلاحيات في لوحة إدارة الدوري

`onAuthStateChanged` في `admin.js` كان ينادي `enterApp()` لأي مستخدم مسجّل، ويفحص السجلّ **فقط حين يكون `LEAGUE_ID` فارغًا**:

```js
onAuthStateChanged(auth, async (user) => {
  if(user) {
    if(!LEAGUE_ID) { /* الفحص الوحيد — لا ينفَّذ غالبًا */ }
    enterApp();          // ← يدخل بلا أي تحقق
  }
});
```

جلسة Firebase محفوظة، فمنظّم البطولة (أ) يفتح `league-admin.html?id=بطولة-ب` فيدخل لوحة غيره كاملة. والمنظّم الموقوف (`active:false`) كان يدخل كالمعتاد.

**لماذا لا تكفي القواعد وحدها:** ترفض كتابته نعم، لكنه يرى اللوحة بكل بياناتها وأزرارها فيظنّها بطولته، وكل ضغطة ترجع `permission-denied` غامضة. وفي أي نافذة تكون فيها القواعد ناقصة (وهو الحال اليوم) → اختراق فعلي.

**⚠️ الأهم:** هذا هو المسار الوحيد لكل مستخدم عائد — `doLogin` (الذي يفحص بشكل صحيح) لا يعمل إلا في أول دخول.

**✅ أُصلح** — نفس فحص `doLogin` طُبِّق على استعادة الجلسة، مع فحص `active !== false`، واحتياط لمالك البطولة عبر `ownerUid`.

---

## 🔴 ح-4 — Service Worker يُسقط ملفات JS بالكامل

في `sw.js:152`:

```js
if (script && out && _looksLikeHtml(out)) return null;
```

الـ Promise المُمرَّر لـ `respondWith` يجب أن يُنهى بكائن `Response`. فـ `null` يجعل المتصفح يعتبر الطلب **فشل شبكة** فلا يُحمَّل الملف إطلاقًا — بدل أن يتجاوز النسخة الفاسدة ويجلب السليمة.

**الحارس أسوأ من العطل الذي كُتب لأجله:** بدل «نسخة قديمة تعمل»، النتيجة «ميزة ميتة تمامًا» أو صفحة بيضاء.

**✅ أُصلح** — تُحذف المدخلة المسمومة من الكاش ويُعاد الجلب من الشبكة.

---

## 🔴 ح-5 — كسر الكاش معطّل (المستخدم العائد على نسخة قديمة)

`admin.js` (1.1 ميجا، عُدّل 8 سبتمبر) كان يُخدَم بـ `?v=333`. ومعه:

`cards-system.js` (8 سبتمبر) · `admin-matches-tabs.js` (7 سبتمبر) · `live-page-stats-fix.js` (5 سبتمبر) · `health-check.js` (2 سبتمبر) · `back-nav.js` (1 سبتمبر)

المستخدم العائد يبقى على النسخة القديمة من المتصفح — **وتعود الأخطاء المُصلَحة**. وهذا بالضبط ما يحذّر منه التعليق في رأس `sw.js`.

وصفحة الجمهور كانت أسوأ: معظم سكربتاتها بلا أي رقم إصدار.

**✅ أُصلح** — 23 سكربت في لوحة الإدارة + 15 في صفحة الجمهور، كلها على `?v=338`.

---

## 🔴 ح-6 — كلمات المرور نصًّا صريحًا (لم يُصلَح — يحتاج قرارك)

`subscribe.html:769` يخزّن كلمة مرور العميل **بلا تشفير** في Firestore:

```js
const payload = { name, phone, email, password: $('pass').value, ... };
await addDoc(collection(db,'subRequests'), payload);
```

وأسوأ — `summary()` في السطر 828 يرسلها في **رسالة واتساب** نصية.

وتتكرر في `handoverLinks`: حقل `pass` يُخزَّن في مستند قاعدته `allow get: if true`. أي أن **رابط التسليم يحمل كلمة مرور الإدارة، ولا ينتهي صلاحيته أبدًا**. إن أُعيد توجيهه في مجموعة واتساب، تبقى مكشوفة للأبد.

**لم أُصلحها لأنها تمسّ تدفق العمل كاملًا** — الحل في قسم التوصيات (ت-1).

---

# القسم الثاني: المشاكل العالية

## 🟠 ع-1 — «الدوري الموحّد» (swiss) بلا توليد جدول إطلاقًا

العطل مزدوج فلم ينجُ منه أي مسار:

1. **الزرّ اليدوي:** `#swissGenBtn` في `league-admin.html:460` ينادي `swissGenerateFixtures()` — **غير معرّفة في أي ملف من الـ35**. اسمها مُدرَج في قائمة `NAMES` لحارس النقر المزدوج (`admin.js:19195`) فبدت «موجودة»، لكن الضغط كان يرمي `ReferenceError` صامتًا.
2. **التوليد التلقائي:** `_lgAutoGenInner` يخرج فورًا إذا `type !== 'league'`، والموزّع `_autoGenerateMatchesIfReady` يعالج `league`/`groups`/`knockout` فقط.

النتيجة: منظّم بطولة «دوري موحّد» **لا يستطيع توليد جدول إطلاقًا**.

**✅ أُصلح (الزرّ اليدوي) — بإعادة استخدام نظام موجود، لا نظام جديد.**
فحصت `autoSchedule` (سطر 6015): خوارزمية round-robin محايدة تمامًا تجاه نوع البطولة، تعمل على مصفوفة `teams` وحدها، وتحترم `legMode` للذهاب/الإياب، وتمرّ بـ `_lightMatch` فتأخذ القيم الافتراضية. وهذا بالضبط ما يحتاجه الدوري الموحّد.

**⚠️ التوليد التلقائي متروك عمدًا** — `autoSchedule` تعرض نافذة تأكيد، فوصلها بالمسار التلقائي يُقحم نافذة في لحظة لا يتوقعها المنظّم. هذا **تغيير سلوك لا إصلاح عطل**. التوصية: استخراج نواة صامتة (مثل `_lgAutoGenInner`) ووصلها في تحديث مستقل ومُختبَر.

---

## 🟠 ع-2 — الإشعارات تتعطّل للزوّار العائدين

`viewer.js:6247` يستعمل `setDoc` على معرّف التوكن:

- أول تسجيل = `create` → ينجح ✅
- كل إعادة تسجيل (نفس التوكن) = **`update`** → والقاعدة كانت `allow read, update, delete: if false` → يُرفض ❌

والخطأ مبتلَع في `catch` بـ `console.warn` فقط — **فشل صامت تام**.

**✅ أُصلح** — أُضيف `allow update` مع تثبيت أن التوكن لا يتغيّر.

---

## 🟠 ع-3 — تسريب توكنات إشعارات الجمهور

قواعد Firestore **تُجمَع بـ OR**: كل مسار مطابق يُمنح صلاحيته. فهذه القاعدة:

```
match /{sub}/{docId} { allow read: if true; }
```

كانت تُبطل `allow read: if false` المكتوبة لـ `notifTokens` أعلاها — فتوكنات إشعارات كل الجمهور مقروءة للعالم. ونفس الشيء لـ `photoTrash`.

**✅ أُصلح** — استثناء صريح: `allow read: if !(sub in ['notifTokens','photoTrash'])`. هذه هي الطريقة الصحيحة الوحيدة، لأن الـ «deny» لا يُبطل «allow» في Firestore.

---

## 🟠 ع-4 — تسريب بيانات تجارية بين المنظّمين

`subscriptions read: if signedIn()` يعني أن **أي منظّم بطولة واحدة يقرأ اشتراكات كل عملاء المنصة**: بريدهم، مبالغهم، تواريخهم، خططهم.

**✅ أُصلح** — حُصرت القراءة على صاحب الاشتراك + السوبر أدمن، مع دعم الشكلين (`sid == leagueId` أو حقل `leagueId`) حتى لا تنكسر البيانات القائمة. تحققت أن `where('leagueId','==',LEAGUE_ID)` في `admin.js:10874` يبقى صالحًا.

---

## 🟠 ع-5 — البطولات القديمة قد تُرفض كل تعديلاتها

قاعدة `update` كانت تقرأ `resource.data.ownerUid` مباشرة. في Firestore، الوصول لحقل غائب **يرمي خطأ تقييم → رفض**. فأي بطولة أُنشئت قبل إضافة `ownerUid` تُرفض كل تعديلاتها بلا سبب ظاهر — بما فيها `quotaAlert` الذي يكتبه `subscription-quota.js`.

**✅ أُصلح** — استُبدلت بـ `.get('ownerUid','')` فتقارن «الغائب بالغائب» وتنجح، وتظل تمنع **تغيير** المالك متى وُجد.

---

# القسم الثالث: المشاكل المتوسطة

## 🟡 م-1 — ميزات ميتة لا تظهر أبدًا

| العنصر | الموضع | الحالة |
|---|---|---|
| `sponsorStrip` (مساحة الراعي الرئيسي) | `league-viewer.html:109` | لا كود يعرضها أو يملؤها، و`_openSponsorLink` غير معرّفة |
| `qeOverlay` («إدخال سريع») | `league-admin.html:1253` | **صفر** إشارات إليه في المشروع كله |

لا تسبّبان خطأً (كلاهما `display:none` و`sponsorStrip` محروس بـ `&&`)، لكنهما ميزتان مدفوعتا التكلفة وغير مُنجَزتين.

## 🟡 م-2 — معاينة الروابط على واتساب معطّلة

وسوم `og:` في صفحة الجمهور **ثابتة لكل البطولات**: كل رابط يُشارَك يظهر بعنوان «منصة بطولات» العام — لا اسم البطولة.

وأسوأ: `og:image` مسار **نسبي** (`icon-512.png`). معظم الزاحفات (واتساب، تويتر، فيسبوك) تتطلب رابطًا مطلقًا — **فلا تظهر صورة إطلاقًا**.

**لم أُصلحه:** النطاق الحقيقي غير موجود في المشروع. يحتاج قيمة مطلقة منك، والحل الكامل في (ت-3).

## 🟡 م-3 — لوحات الإدارة قابلة للفهرسة
`league-admin.html` · `superadmin.html` · `broadcaster.html` بلا `noindex`.
**✅ أُصلح** — أُضيف `noindex,nofollow` للثلاثة.

## 🟡 م-4 — روابط التسليم بلا انتهاء صلاحية
`handoverLinks` تستخدم `crypto.randomUUID()` (122 بت — قوي ✅)، لكن بلا `expiresAt` ولا TTL. ومع احتوائها على كلمة المرور (ح-6)، الرابط المُسرَّب يبقى صالحًا للأبد.

## 🟡 م-5 — أي زائر يحذف مقاعد مشاهدي البث
`liveStreams/{sid}/viewers/{vid}` قاعدتها `allow create, update, delete: if true` بلا أي قيد. أي زائر يحذف مقاعد كل المشاهدين الآخرين فيقطع بثّهم. الأثر محدود (إشارة WebRTC فقط) لكنه متجه تعطيل سهل.

## 🟡 م-6 — هشاشة التحميل بالاستقصاء
ملفات الترقيع تنتظر النواة بـ `setTimeout` مع حدّ محاولات (`tries > 500` في `subscription-quota.js`). على جهاز بطيء أو شبكة ضعيفة، تتجاوز المهلة **فتموت الميزة بصمت بلا أي أثر في الـ console**.

---

# القسم الرابع: ما تم إصلاحه

| # | الإصلاح | الملف | الخطورة |
|---|---|---|---|
| 1 | `isSuperAdmin()` يفحص `admins/{uid}.role` بدل غياب سجلّ | `firestore.rules` | 🔴 حرجة |
| 2 | إضافة قاعدة `admins` المفقودة (إصلاح دخول السوبر أدمن) | `firestore.rules` | 🔴 حرجة |
| 3 | إضافة قاعدة `settings` المفقودة | `firestore.rules` | 🔴 حرجة |
| 4 | السماح للسوبر أدمن بإنشاء/حذف البطولات والمنظّمين والاشتراكات | `firestore.rules` | 🔴 حرجة |
| 5 | التحقق من ملكية البطولة عند استعادة الجلسة | `admin.js` | 🔴 حرجة |
| 6 | حذف النسخة المسمومة والرجوع للشبكة بدل `return null` | `sw.js` | 🔴 حرجة |
| 7 | توحيد 38 سكربتًا على `?v=338` | `league-admin.html`, `league-viewer.html` | 🔴 حرجة |
| 8 | تعريف `swissGenerateFixtures` بربطها بـ `autoSchedule` | `admin.js` | 🟠 عالية |
| 9 | السماح بـ `update` على `notifTokens` (إصلاح الإشعارات) | `firestore.rules` | 🟠 عالية |
| 10 | استثناء `notifTokens`/`photoTrash` من قاعدة الـ wildcard | `firestore.rules` | 🟠 عالية |
| 11 | حصر قراءة `subscriptions` + تحصين `ownerUid` للمستندات القديمة | `firestore.rules` | 🟠 عالية |
| 12 | `noindex` للوحات الإدارة الثلاث | 3 صفحات | 🟡 متوسطة |

**التحقق:** كل ملفات JS الـ35 تمرّ `node --check` بعد التعديل. قواعد Firestore متوازنة الأقواس (55/55). كل تعديل موثّق بتعليق عربي يشرح الخلل وسببه ولماذا اختير هذا الحل.

### ملاحظة تصميمية مهمة في القواعد

رُتِّبت `isSuperAdmin()` **آخرًا** في `canManage()` عمدًا:

```
((ownsLeague(lid) || isOwnerUid(lid)) && leagueActive(lid)) || isSuperAdmin()
```

كل `get()` داخل القواعد **قراءة مُحاسَبة**، والمنظّم هو 99٪ من الكتابات. وضعها أولًا كان سيفرض قراءة إضافية على كل كتابة في المنصة كلها.

---

# القسم الخامس: مخاطر التوسّع

> هذه لم تُعدَّل — تحتاج قرارك لأنها تمسّ منطق العرض الحيّ.

## ⚡ ت-أ — أكبر خطر تكلفة في المنصة

`viewer.js:752` يفتح مستمع `onSnapshot` لكشف **كل فريق** فور التحميل:

```js
teams.forEach(t => _ensureRosterLoaded(t.id));
```

**الحساب:** بطولة 32 فريقًا = 32 مستمع كشوف + مباريات + فرق + مجموعات + شجرة + إعدادات ≈ **37 مستمعًا دائمًا لكل مشاهد**. ومع 5,000 مشاهد متزامن = **185,000 مستمع مفتوح**.

**وأسوأ:** `_rosterListeners` **لا يُلغى أبدًا** — لا `unsubscribe` في أي موضع من الملف.

**الحل المقترح:** تحميل الكشوف عند الطلب (فتح الفريق/المباراة) بـ `getDocs` المدعوم بالكاش الدائم المُفعَّل سلفًا، وإبقاء `onSnapshot` للمباراة المفتوحة وحدها. والمكسب الإضافي: زمن تحميل أول أسرع بوضوح.

## ⚡ ت-ب — تحميل المباريات بلا حدّ
`onSnapshot` على كل المباريات بلا `limit`. دوري بـ 500 مباراة يُنزَّل كاملًا لكل مشاهد في كل فتح بارد. الحل: `limit` + ترقيم حسب الجولة.

## ⚡ ت-ج — كل كتابة تكلّف قراءتين
`canManage()` ينفّذ `get()` على `leagueAdmins/{uid}` و`leagues/{lid}`. **الحل الجذري: Custom Claims** على توكن المصادقة (`leagueId` + `role` + `active`) — يُلغي كل `get()` من القواعد فتصير الكتابة بتكلفة صفر قراءة، ويُبسّط `isSuperAdmin()` إلى `request.auth.token.role == 'superadmin'`.

---

# القسم السادس: التوصيات المستقبلية

> التزمت بطلبك: **فحصت أولًا هل الميزة موجودة** — وكل ما يلي إمّا إكمال لموجود أو معالجة لخلل جذري، لا نظام مكرر.

## ت-1 — نقل عمليات السوبر أدمن إلى Cloud Functions ⭐ الأولوية القصوى

إنشاء الحسابات وكلمات المرور من المتصفح هو **أصل** ثغرة التصعيد (ح-2) ومشكلة كلمات المرور (ح-6). مع Admin SDK:

- تعود `write: if false` صحيحة على `leagueAdmins` و`subscriptions` و`leagues`
- تختفي الحاجة لتخزين كلمات المرور نصًّا — تُنشأ مؤقتة وتُجبَر على التغيير أول دخول
- يمكن استخدام رابط إعادة تعيين Firebase الرسمي بدل إرسال كلمة مرور على واتساب

هذا يُغلق ثلاث مشاكل بحلّ واحد.

## ت-2 — إكمال ميزات موجودة (لا بناء جديد)

- **`sponsorStrip`:** نظام الرعاة موجود سلفًا على مستوى المباراة (`m.sponsor` + `showSponsor` في `viewer.js:8809`). التوصية: **رفعه لمستوى البطولة** وتوصيله بالشريط الميت — لا نظام رعاية جديد.
- **التوليد التلقائي لـ swiss:** استخراج نواة صامتة من `autoSchedule` ووصلها بالموزّع (تفصيل في ع-1).
- **نظام التوقّعات:** فحصته — `predictions.js` يحفظ في `localStorage` فقط، بلا خادم وبلا تكلفة. **تصميم ممتاز.** لو أردت لوحة متصدّرين مستقبلًا، فهي تطوير لهذا النظام لا نظام جديد.

## ت-3 — معاينة روابط ديناميكية ⭐ أعلى عائد تسويقي

كل رابط بطولة يُشارَك اليوم يظهر بعنوان عام وبلا صورة. الحل: Cloud Function (أو Hosting rewrite) تحقن `og:title` باسم البطولة و`og:image` بشعارها **كرابط مطلق**. في منصة رياضية يحدث نموّها بالمشاركة، هذا أقل جهد لأعلى أثر.

## ت-4 — صفحة رئيسية حقيقية
جذر الموقع اليوم `index.html` = صفحة خطأ «رابط غير صحيح» بـ `noindex`. لا واجهة تعريفية ولا مسار للتسجيل من الجذر.

## ت-5 — تقليص `admin.js`
1.1 ميجابايت / 19,400 سطر في ملف واحد يُحمَّل كاملًا في كل فتح. تقسيمه لوحدات محمّلة عند الطلب (`import()` الديناميكي) يقلّص زمن الفتح الأول بوضوح — أهم مقياس على الجوال.

## ت-6 — بيئة اختبار منفصلة
مشروع Firebase ثانٍ (staging) لاختبار القواعد والتعديلات قبل الإنتاج. مع آلاف المستخدمين، نشر قواعد غير مُختبَرة مباشرة على الإنتاج مخاطرة لا تُحتمَل — وهي بالضبط ما كان سيحدث مع النسخة الحالية.

---

# ⚠️ قبل النشر — خطوات إلزامية

1. **أنشئ مستند `admins/{uid}`** بحقل `role: "superadmin"` لحسابك من Firebase Console.
   **بدونه لن تدخل اللوحة بعد نشر القواعد الجديدة.**

2. **اختبر القواعد في Rules Playground** أو المحاكي المحلي قبل `firebase deploy`:
   ```bash
   firebase emulators:start --only firestore
   ```
   سيناريوهات الاختبار الدنيا:
   - دخول السوبر أدمن + إنشاء بطولة + إنشاء منظّم + إنشاء اشتراك
   - منظّم يكتب في بطولته ✅ / في بطولة غيره ❌
   - منظّم ببطولة مقفلة (`locked:true`) ❌
   - زائر يقرأ المباريات ✅ / يقرأ `notifTokens` ❌
   - بطولة قديمة بلا `ownerUid` — تعديل المنظّم ✅

3. **انشر القواعد والملفات معًا.** نشر الملفات وحدها يُبقي الثغرات؛ ونشر القواعد وحدها آمن لكن قد يكشف أخطاء واجهة.

4. **راقب `console` بعد النشر** — إن ظهرت `permission-denied` غير متوقعة، القواعد قابلة للرجوع فورًا من النسخة الاحتياطية.

---

# ما لم يُفحص بعد

للأمانة، هذه الأنظمة فُحصت سطحيًا فقط وتستحق جولة مخصّصة:

- `broadcaster.html` — منطق WebRTC والتفاوض والتقاط الإعادة
- `cards-system.js` (115KB) — بطاقات المشاركة والتوليد
- `suspensions.js` (59KB) — منطق تراكم البطاقات والإيقافات الآلي
- `tiebreak-rules.js` + منطق الترتيب — **صحة الحسابات الرياضية** (المواجهات المباشرة، فارق الأهداف)
- `health-check.js` (44KB) — نظام الفحص الذاتي
- `penalties-core.js` — ركلات الترجيح
- سلوك المنصة تحت ضعف الشبكة والتزامن الحقيقي بين لوحة التحكم والجمهور

**أوصي بأن تكون الجولة التالية مخصّصة لصحة الحسابات الرياضية** (الترتيب وكسر التعادل والإيقافات) — فخطأ هناك لا يُسقط المنصة، لكنه يُتوّج الفريق الخطأ، وهو أسوأ ما قد يحدث لمنصة بطولات.

---

*انتهى التقرير — v338-audit*

---
---

# ملحق — الجولة الثانية: صحة الحسابات الرياضية

**النطاق:** الترتيب العام · ترتيب المجموعات · كسر التعادل · الإيقافات الآلية · منطق التأهّل

هذه الجولة هي التي أوصيت بها في نهاية التقرير الأول، لأن خطأً هنا لا يُسقط المنصة — بل **يُتوّج الفريق الخطأ**، وهو أسوأ ما قد يحدث لمنصة بطولات.

## خلاصة الجولة

اكتُشف أن المنصة تحتوي على **أربع** نسخ مستقلة لحساب الترتيب، لا تتفق فيما بينها:

| النسخة | الموضع | الحالة قبل الإصلاح |
|---|---|---|
| الجدول العام — الإدارة | `admin.js:3707` `recalcStandings` | ✅ المرجع الصحيح |
| الجدول العام — الجمهور | `viewer.js:5847` `renderStandings` | ❌ ينقصه شرط واحد |
| المجموعات — الجمهور | `viewer.js:1617` `computeGroupStats` | ❌ ينقصه شرط واحد |
| المجموعات — الإدارة | `admin.js:15970` `_computeGroupStats` | ❌ ثلاثة فروق |

---

## 🔴 ر-1 — الجمهور يرى نقاطًا تخالف ما يراه المنظّم

لوحة الإدارة تشترط أن تكون النتيجة **رقمًا**:

```js
typeof m.homeScore === 'number' && typeof m.awayScore === 'number'
```

هذا الشرط كان **غائبًا** عن نسختَي الجمهور. فمباراة موسومة `finished` ونتيجتها `null` (إدخال ناقص، أو إنهاء بلا تسجيل نتيجة) تُعامَل معاملتين متناقضتين:

| | الإدارة | الجمهور |
|---|---|---|
| المباراة | تُتجاهَل | `(null\|\|0)` → **0-0** |
| النقاط | لا شيء | **نقطة لكل فريق** |
| «لعب» | لا تُحتسب | **تُحتسب للفريقين** |

وفي `computeGroupStats` الأمر أخفى: `null > null` و`null < null` كلاهما `false`، فتسقط المباراة إلى الفرع الأخير — **تعادل**.

**✅ أُصلح** — وُحِّد الشرط في النسختين.

---

## 🟠 ر-2 — ترتيب المجموعات في الإدارة: ثلاثة فروق عن الجمهور

`admin.js:15970` كان فلتره `m.status === 'finished'` وحده. النتائج:

1. **مباريات الإقصاء تدخل جدول المجموعة.** فريقان من نفس المجموعة يلتقيان في ربع النهائي، فتُضاف نتيجة الإقصاء إلى جدول مجموعتهما. الجمهور يستبعدها صراحةً.
2. **لا مطابقة بـ `groupId`** — الجمهور يفضّلها حين تكون مسجّلة (أدقّ من الاعتماد على عضوية الفريقين).
3. **خصم النقاط الإداري مُهمَل تمامًا** — فريق عليه خصم يظهر للمنظّم بنقاطه كاملة وللجمهور منقوصة. والمنظّم هو من فرض الخصم فيراه غير مطبَّق في لوحته.

**تقدير الأثر — تحققت قبل التصعيد:** هذا الترتيب يُستعمل في **محرّر توزيع المجموعات فقط**. وفحصت `_getQualifiedPool` (سطر 13816): **التأهّل اختيار يدوي من المنظّم** (`qualifiedTeamIds` + `qualificationPublished`)، لا حساب آلي. فلا فريق يتأهل خطأً تلقائيًا.

لكن أن يرى المنظّم ترتيبًا والجمهور ترتيبًا آخر يهدم الثقة بالأرقام كلها — وهي رأس مال منصة بطولات.

**✅ أُصلح** — طوبقت النسخة على نسخة الجمهور بالكامل، ومُرِّر `groupId` من موضع الاستدعاء.

---

## 🟠 ر-3 — كسر التعادل يناقض الجدول الذي يرتّبه

الجدول يستبعد مباريات الإقصاء صراحةً — في الإدارة والجمهور معًا. لكن **كاسر التعادل الذي يرتّب هذا الجدول نفسه كان يحتسبها**:

- **المواجهات المباشرة (`h2h`):** فلترها `m.status === 'finished'` فقط. فلقاء الفريقين في نصف النهائي كان يغيّر ترتيبهما في الدور الدوري.
- **اللعب النظيف (`cards`):** `_teamCardCount` يعدّ بطاقات **كل** المباريات المنتهية، بما فيها الإقصاء.

تناقض مباشر مع تعريف الجدول، وقادر على قلب ترتيب فريقين متساويي النقاط.

**✅ أُصلح في النسختين** (`viewer.js` و`admin.js`) — أُضيف استبعاد الإقصاء وشرط النتيجة الرقمية، تطابقًا حرفيًا مع فلتر الجدول.

**ملاحظة:** تحققت من مستدعي الدالتين قبل التعديل — `_teamCardCount` يُستعمل حصرًا داخل `applyTiebreak`، و`applyTiebreak` يُستعمل حصرًا لترتيب الجداول. فالتعديل آمن بلا آثار جانبية.

---

## ✅ ما وجدته سليمًا (للتوثيق)

**نظام الإيقافات (`suspensions.js`) — مبنيّ بإتقان.** يعيد تشغيل المباريات زمنيًا، ويخصم الإيقاف **قبل** معالجة أحداث المباراة (وإلا قضى المطرود عقوبته في مباراة طرده نفسها)، ويفصل الطرد بإنذارين عن الحمراء المباشرة، ويدعم مسح الأرصدة عند بداية الإقصاء، والإعفاءات اليدوية مربوطة بمباراة محددة فتصمد أمام إعادة الحساب.

**حساب الترتيب في الإدارة سليم منطقيًا:** ركلات الترجيح **لا تدخل الجدول** (المباراة تعادل — وهو الصحيح قانونًا)، والإقصاء مستبعد، وخصم النقاط يُطرح مرة واحدة بعد الحلقة لا داخلها.

**`applyTiebreak`:** النسختان متطابقتان منطقيًا — لا تباين.

**نظام التوقّعات (`predictions.js`):** `localStorage` فقط، بلا خادم وبلا تكلفة وبلا سطح إساءة. تصميم ممتاز.

### تحقّق منع بلاغًا خاطئًا

لاحظت أن `suspensions.js` يقبل نوع حدث `'secondyellow'` ثم يعالجه في فرع الحمراء المباشرة — ما يعني تطبيق `redBan` بدل `secondYellowBan`. قبل التبليغ فحصت مُنتِجي الأحداث: **المنصة لا تُنتج هذا النوع إطلاقًا** — الطرد بإنذارين يُشتق من عدّ صفراوين (`viewer.js:6716`, `:8173`). فالفرع دفاعي لنوع غير موجود، **لا خطأ فعلي**. أسجّله كملاحظة متانة لا كعطل.

---

## 🟡 ر-4 — ترتيب الإيقافات يعتمد الموعد المجدول لا وقت اللعب الفعلي

`oldestFirst` يرتّب بـ `date + time`، ولا يوجد حقل `finishedAt` في أي مسار كتابة (فحصت الأربعة: `admin.js:3613, 3657, 15184, 18704` — كلها `updatedAt` فقط، وهو يتغيّر مع أي تعديل لاحق).

فلو لُعبت مباراة مؤجَّلة قبل موعدها المسجَّل، تُعاد معالجتها في الترتيب الخطأ — وقد يُحتسب الإيقاف في المباراة الخطأ.

**لم أُصلحه** — إضافة `finishedAt` تمسّ أربعة مسارات كتابة، وهي تغيير تراكمي يستحق تحديثًا مستقلًا ومُختبَرًا.

---

## إصلاحات الجولة الثانية

| # | الإصلاح | الملف |
|---|---|---|
| 13 | شرط النتيجة الرقمية في الجدول العام | `viewer.js` |
| 14 | شرط النتيجة الرقمية في ترتيب المجموعات | `viewer.js` |
| 15 | استبعاد الإقصاء + `groupId` + خصم النقاط في مجموعات الإدارة | `admin.js` |
| 16 | تمرير `groupId` من `_sortGroupTeamsByStandings` | `admin.js` |
| 17 | استبعاد الإقصاء من المواجهات المباشرة (الجمهور) | `viewer.js` |
| 18 | استبعاد الإقصاء من عدّ البطاقات (الجمهور) | `viewer.js` |
| 19 | استبعاد الإقصاء من المواجهات المباشرة (الإدارة) | `admin.js` |
| 20 | استبعاد الإقصاء من عدّ البطاقات (الإدارة) | `admin.js` |

**المحصلة:** أربع نسخ للترتيب صارت متطابقة الفلتر حرفيًا. كل ملفات JS الـ35 تمرّ `node --check`.

---

## توصية معمارية — مصدر حقيقة واحد للترتيب

وجود **أربع** نسخ لحساب الترتيب هو أصل كل ما سبق: كل إصلاح يجب أن يُطبَّق أربع مرات، وكل نسيان يخلق تباينًا جديدًا بين ما يراه المنظّم وما يراه الجمهور.

**المقترح:** استخراج وحدة واحدة (`standings-core.js`) على غرار `match-core.js` الموجود سلفًا في المشروع — بدالة واحدة `computeStandings(matches, teams, settings, opts)` تستعملها الأربع. لا نظام جديد، بل توحيد لأربعة موجودة.

هذا أعلى استثمار في استقرار المنصة على المدى الطويل، ويجب أن يتم **بعد** اختبار إصلاحات هذه الجولة لا معها.

---

## ما زال خارج الفحص

`broadcaster.html` (منطق WebRTC) · `cards-system.js` (115KB) · `health-check.js` (44KB) · `penalties-core.js` · سلوك التزامن الحقيقي تحت ضعف الشبكة.

*انتهى الملحق — v338-audit — الجولة الثانية*

---
---

# ملحق — الجولة الثالثة: البث والفحص الذاتي وبطاقات المشاركة

**النطاق:** `broadcaster.html` · `health-check.js` · `cards-system.js`
وهي الأنظمة التي بقيت خارج الجولتين السابقتين.

---

## 🔴 ب-1 — عدّاد المشاهدين يتضخّم ولا يعود (ويُنشر للجمهور)

الإنقاص الوحيد كان عند حذف مستند المشاهد من Firestore (`ch.type==='removed'`). لكن انهيار الاتصال — إغلاق التبويب فجأة، انقطاع شبكة الجوال، أو تعليق الصفحة في الخلفية — **يترك المستند قائماً**، فيسقط الإنقاص تماماً.

وأسوأ: عودة نفس المشاهد تُطلق `connected` ثانيةً فيُحسب مرتين، بلا أي إنقاص مقابل.

**لماذا هذا مهمّ:** العدد **يُنشر للجمهور** (`watchers` في مستند البث). فيرى الجميع رقماً متضخّماً لا يمتّ للواقع. وعند تجاوز ٤٠ يُطلق النظام تحذير «عدد كبير — فعّل بث HLS» بلا سبب حقيقي، فيُدفع المذيع لقرار خاطئ بناءً على رقم وهمي.

**✅ أُصلح** — علم واحد لكل اتصال (`pc._counted`) يضمن حسبة واحدة وإنقاصاً واحداً مهما تكرّرت تقلّبات الحالة.

---

## 🟠 ب-2 — الاتصالات المنهارة لا تُغلق

عند `failed`/`closed`/`disconnected` كان `delete viewerPeers[viewerId]` وحده يزيل المرجع، **ويترك `RTCPeerConnection` مفتوحاً** بمرسلاته ومرشّحاته.

في مباراة من ٩٠ دقيقة يتراكم العشرات على جهاز المذيع حتى يثقل البث نفسه — والمذيع غالباً على جوال.

**✅ أُصلح** — `pc.close()` قبل الحذف.

---

## 🟠 ب-3 — مستمع ICE لكل مشاهد لا يُلغى أبداً

```js
onSnapshot(vcandCol, s => { ... });   // بلا أي مرجع للإلغاء
```

مستمع Firestore دائم **لكل من دخل البث ولو لثانية واحدة**. بثّ واحد يستقبل مئات الدخول والخروج خلال مباراة، فتتراكم مئات المستمعين على جهاز المذيع — تكلفة قراءات وذاكرة معاً.

**✅ أُصلح** — تُحفظ دالّة الإلغاء على الاتصال (`pc._unsubIce`) وتُستدعى عند الانهيار وعند حذف المستند.

---

## 🟠 ص-1 — حذف جماعي بلا موافقة عند تعذّر نظام التأكيد

في `hcAutoFixAll` (إصلاح كل ما يمكن إصلاحه):

```js
var ok = window.confirmDialog ? await window.confirmDialog({...}) : true;
```

الاحتياطي `true` يعني: لو تعذّر تحميل نظام التأكيد لأي سبب، **يُنفَّذ حذف جماعي للمباريات المكرّرة بلا أي سؤال**. وهو أخطر مسار في اللوحة كلها (`danger: true`).

المسار الفردي `hcAutoFix` يسقط إلى `confirm()` الأصلي بشكل صحيح — فالخلل في المسار الجماعي وحده، وهو الأخطر.

**✅ أُصلح** — يسقط إلى `confirm()` مثل نظيره الفردي.

---

## ✅ ما وجدته سليمًا

**نظام الفحص الذاتي (`health-check.js`) مصمَّم بمسؤولية.** كل إصلاح يعرض نافذة تشرح بالضبط ما سيُحذف وكم عنصراً وما الذي لن يُمسّ. ولا يُقدَّم إصلاح تلقائي إلا حين يكون له جواب واحد لا لبس فيه. وحذف المكرّرات يُبقي الأقدم ومَن له نتيجة. و`silent` لا يتجاوز الموافقة — بل يستعملها `hcAutoFixAll` بعد موافقة واحدة تسرد كل البنود.

**البث:** إعداد ICE سليم (٤ خوادم STUN)، وتحسين صوت Opus، وتوزيع جودة تكيّفي حسب عدد المشاهدين.

### تحقّقان منعا بلاغين خاطئين

**① تلويث الـ Canvas في بطاقات المشاركة.** `toDataURL` يقع **خارج** `try/catch`، فبدا أن فشل CORS على شعار فريق سيُسقط توليد البطاقة بلا رسالة. لكن `loadImg` يضبط `crossOrigin='anonymous'` ويرجع `null` عند الفشل **ولا يعيد المحاولة بلا CORS** — فلا سبيل لتلويث الـ Canvas أصلاً. لا خطر فعلي.

**② `secondyellow`** (من الجولة الثانية) — نوع حدث لا تُنتجه المنصة إطلاقاً.

هذا ثالث بلاغ محتمل أوقفه التحقّق قبل التصعيد.

---

## 🟡 ب-4 — TURN معطّل (قرار منتج لا خطأ)

`TURN_CONFIG` فارغ (أسطر معلّقة بـ `YOUR_USER`/`YOUR_CRED`)، فالبث يعتمد على STUN وحده.

**الأثر:** المشاهد خلف NAT متماثل أو على شبكة جوال مقيّدة **لا يستطيع الاتصال إطلاقاً** — لا بثّ متقطّع، بل فشل كامل. النسبة المعتادة ١٠–٢٠٪ من المشاهدين، وترتفع على شبكات الجوال العربية.

المنصة تعرف ذلك سلفاً (مؤشّر `TURN_ENABLED` موجود). القرار لك: خادم TURN مدفوع، أو الاكتفاء بـ HLS للبثوث الكبيرة.

---

## إصلاحات الجولة الثالثة

| # | الإصلاح | الملف |
|---|---|---|
| 21 | عدّاد المشاهدين: حسبة واحدة وإنقاص مضمون | `broadcaster.html` |
| 22 | إغلاق الاتصالات المنهارة | `broadcaster.html` |
| 23 | إلغاء مستمع ICE لكل مشاهد | `broadcaster.html` |
| 24 | منع الحذف الجماعي بلا موافقة | `health-check.js` |

---

## الحصيلة الكلية للجولات الثلاث

| | |
|---|---|
| ملفات مفحوصة | 36 ملف JS · 7 صفحات |
| إصلاحات منفَّذة | **24** |
| ألوان موحَّدة على الهوية | 208 في 6 ملفات |
| بلاغات خاطئة أوقفها التحقّق | 3 |

---

## ما زال خارج الفحص

`substitution-system.js` · `predictions.js` (فُحص سطحياً — سليم) · `match-share-card.js` · `groups-gate.js` · `clock-sync.js` · سلوك التزامن الحقيقي تحت ضعف الشبكة (يحتاج اختباراً حيّاً لا قراءة شيفرة).

*انتهى الملحق — v338-audit — الجولة الثالثة*
