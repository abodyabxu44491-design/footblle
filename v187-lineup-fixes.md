# v187 — تطوير التشكيلة: إطار نظيف + ربط أحداث اللاعب بالهوية

> ملف تجميعي كامل لكل التعديلات في هذا الإصدار. يتبع نمط `FIX-NOTES.md`.
> **الملفات المعدّلة:** `viewer.js` · `viewer.css` · `sw.js`

---

## المشكلتان اللتان عولجتا

### ① التشكيلة — إطار اللاعب كبير/باهت (شكل غير احترافي)
كل لاعب على الملعب كان عليه **إطار متدرّج سميك ملوّن** (ذهبي للفريق / أحمر للخصم /
بنفسجي للحارس) بحجم `padding:2.5px` + ظل قوي `0 5px 14px` + طبقة حدّ إضافية. النتيجة:
- الملعب مزدحم بإطارات ملوّنة سميكة على الجميع.
- **رجل المباراة لا يتميّز** فعلياً لأن كل اللاعبين ذهبيون أصلاً.
- شارة الرقم ذهبية أيضاً فتندمج بصرياً مع الإطار.

### ② خصم/إضافة الهدف على اللاعب في التشكيلة لا يعمل عند التعديل
شارات الأحداث على الملعب (⚽ هدف · 🟨 أصفر · 🟥 أحمر) كانت تُطابق اللاعب
**بالاسم أو الرقم فقط** (`_playerMatchBadges`)، لا بالهوية `playerId`. النتيجة:
- عند **نقل هدف** من لاعب لآخر (تعديل صاحب الهدف) → الشارة تبقى على اللاعب القديم.
- عند وجود **لاعبَين بنفس الاسم** → الهدف قد يظهر على الخطأ.
- المطابقة بالرقم/الاسم هشّة أمام تعديل الاسم أو تشابهه.

---

## الإصلاحات

### الإصلاح ① — إعادة تصميم قرص اللاعب (viewer.js + viewer.css)

**الفلسفة الجديدة (كالتطبيقات الرسمية):**
- **إطار ذهبي خفيف جداً وناعم (1.5px)** موحّد لكل اللاعبين عبر
  `box-shadow: 0 0 0 1.5px rgba(230,205,140,.42)` — بالكاد يُلاحَظ، مجرد حدّ رقيق
  يفصل اللاعب عن العشب دون أن يلفت الانتباه (بديل الأبيض القوي الأول، وبديل الإطار
  المتدرّج السميك القديم).
- **الحارس:** حلقة بنفسجية خفيفة إضافية (`0 0 0 3px rgba(168,107,214,.34)`) — تمييز
  لطيف بلا فجاجة.
- **رجل المباراة (MOTM) فقط:** إطار ذهبي متوهّج
  (`0 0 0 2px #e6c157, 0 0 10px rgba(230,193,87,.75)`) + نجمة ★ — يتميّز فوراً وسط
  الإطارات الخفيفة.
- **بنية جديدة:** القرص صار `overflow:hidden` (الصورة تملأه بلا تشويه)، والرقم/الكابتن/
  النجمة/الشارات نُقلت إلى **طبقة overlay منفصلة** فوق القرص (وإلا لاختفت بسبب القصّ).
- **شارة الرقم** صارت **بيضاء** (`background:#fff;color:#111`) بدل الذهبية — أوضح وأنظف.
- نفس التصميم النظيف طُبّق على **مقاعد البدلاء** (إطار أبيض 1.5px) للاتساق.

**التغييرات في viewer.css:**
- `.pl-badges` — تحسين الموضع (`top:-6px; left:-6px; gap:2.5px; z-index:7`) لترتيب أوضح
  للشارات بعيداً عن الرقم (يمين-أسفل) والنجمة (يمين-أعلى) والكابتن (يسار-أسفل).

### الإصلاح ② — ربط شارات الأحداث بالهوية (playerId) في التشكيلة

**`_playerMatchBadges(events, side, playerName, number, playerId)`** — أُضيف معامل خامس
`playerId`، وأُعيد بناء المطابقة بأولوية الهوية:

```
المطابقة الجديدة (matchesPlayer):
  ① الحدث له هوية + اللاعب له هوية  → المطابقة بالهوية وحدها (الأدقّ)
  ② الحدث له هوية + اللاعب بلا هوية → لا مطابقة (تجنّب الخطأ)
  ③ الحدث بلا هوية (بيانات قديمة)   → رجوع آمن للرقم، ثم الاسم المُطبّع
```

طُبّق على الأهداف والبطاقات **والتبديلات** (`playerOutId` / `playerInId`).

**مواقع الاستدعاء المُحدّثة (تمرير `p.id`):**
- الأساسيون على الملعب (سطر ~4748).
- البدلاء في مقاعد البدلاء (سطر ~4803).

**النتيجة — الحلقة الكاملة تعمل بالهوية:**
| الإجراء | قبل | بعد |
|---|---|---|
| نقل هدف من لاعب لآخر (بث/بعد المباراة) | يبقى على القديم | **يُخصم من القديم ويُضاف للجديد فوراً** |
| حذف هدف | قد يبقى بالاسم | **يختفي فوراً** |
| لاعبان بنفس الاسم | قد يختلطان | **مفصولان تماماً بالهوية** |
| بيانات قديمة بلا هوية | تعمل بالاسم | **تعمل كما هي (رجوع آمن)** |

> **لماذا تعمل تلقائياً؟** الشارات تُحسب لحظياً من `m.events`. وعند أي تعديل من الإدارة،
> مستمع `onSnapshot` في الجمهور يعيد استدعاء `openMatchDetail` (viewer.js سطر ~181)،
> فتُعاد التشكيلة بالرسم بأحدث الأحداث. كل مسارات الإدخال (بث/سريع/QR) والتعديل
> (`lpSaveEditEvent`) تخزّن `playerId` أصلاً — لم يكن ينقص إلا أن يقرأه العرض.

### الإصلاح ③ — رفع نسخة الكاش (sw.js) [إلزامي]
`batolat-v84 → batolat-v87`. بحسب درس **v175**: أي تعديل على `viewer.js/css` لا يظهر
للجمهور إطلاقاً ما لم تُرفع نسخة `sw.js` (لأن Service Worker يقدّم النسخة المخزّنة).

---

## الاختبارات

**اختبار منطقي معزول لـ `_playerMatchBadges` (١١/١١ نجح):**
- هدف بهوية A يظهر على A فقط، لا على B (نفس الاسم، هوية مختلفة).
- نقل الهدف من A إلى B: يختفي عن A ويظهر على B.
- حذف الهدف: لا شارة لأحد.
- بيانات قديمة بلا هوية: مطابقة بالرقم؛ رقم مختلف لا يطابق.
- حدث بهوية + لاعب بلا هوية → لا مطابقة (تجنّب الخطأ).
- لاعب بهوية + حدث قديم بنفس الاسم → مطابقة بالاسم (رجوع آمن).
- هدفان + كرت بنفس الهوية → عدّاد صحيح.
- هدف للفريق الخصم لا يظهر على لاعب فريقنا.

**تصيير بصري فعلي (Chromium):** أُنشئت صور «قبل/بعد» بتشكيلة ١١ لاعباً فيها حارس،
لاعب بصورة، لاعب بلا صورة، رجل مباراة، لاعب سجّل هدفَين، ولاعبان بكرت أصفر/أحمر —
تأكّد الشكل الاحترافي في حجمَي الأفاتار (42px و56px).

**فحص الصياغة:** `node --check` نجح لـ `viewer.js` · `sw.js` · `admin.js` ·
`admin-lineup-dragdrop.js`.

---

## النشر
ارفع الملفات الثلاثة معاً:
1. `viewer.js`
2. `viewer.css`
3. `sw.js` ← **لا تنسه، وإلا يبقى الجمهور على النسخة القديمة.**

## اقتراح متابعة (لم يُنفَّذ بعد)
تطبيق نفس تصميم الإطار الأبيض النظيف على **ملعب الإدارة** (`admin-lineup-dragdrop.js`)
ليرى المنظّم الشكل نفسه أثناء ترتيب التشكيلة (حالياً الإدارة تبقى بالتصميم القديم عمداً
لدقّة السحب والإفلات).
