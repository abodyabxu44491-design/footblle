# v337 — ركلات الترجيح: النتيجة الرسمية لا تُدهس

## 🔴 أصل المشكلة كلها: خلل في الحفظ

`_lpSaveV2` كان يكتب في قاعدة البيانات:

```js
homeScore: st.penHomeScore != null && st.penalties
           ? st.penHomeScore      // ← نتيجة الترجيح!
           : st.homeScore
```

**مباراة انتهت ٢-٢ وحُسمت ٤-٣ بالركلات كانت تُحفظ بنتيجة ٤-٣.**
التعادل يُمحى من الحقل نهائياً، ويصير رقم الترجيح هو «نتيجة المباراة»
في كل مكان يقرؤها.

ومن هنا جاء **التكرار الذي رأيته في البطاقات**: الرقم الكبير صار ٤–٣
(وهو الترجيح لا المباراة)، ثم يُرسم تحته «(ركلات الترجيح: ٤–٣)» مرة
ثانية — فيظهر الرقم نفسه مرتين، ولا أثر للتعادل أصلاً.

ويفسد معه كل ما يُبنى على النتيجة: أسماء الهدّافين المشتقّة من الأحداث
تقول هدفين والنتيجة تقول أربعة.

**الإصلاح:** لكل رقم مكانه — `homeScore` للمباراة،
و`penaltyScoreHome/Away` للترجيح، و`liveData.penalties` للركلات.

### واستعادة ما فسد

المباريات التي حُفظت خطأً من قبل يظهر لها **شريط استعادة** في صفحة
المباريات. الكشف بسيط ومؤكَّد: **ركلات ترجيح موجودة والنتيجة ليست
تعادلاً** — وهذا مستحيل قانونياً، فلا تُضرب ركلات ترجيح إلا بعد تعادل.

والاستعادة من **سجلّ الأحداث**: الأهداف محفوظة حدثاً حدثاً، فتُعاد
النتيجة الأصلية منها بلا تخمين (والأهداف الملغاة مستثناة تلقائياً).
والمباريات بلا أحداث محفوظة تُحصى وتُطلب يدوياً بدل تخمين رقم لها.

---

## بقية ما أُصلح

### ① بطاقة الجمهور أثناء الترجيح

في طور ركلات الترجيح كانت البطاقة تعرض نتيجة **الترجيح** بالخطّ الكبير
(٣:١) وتُنزل نتيجة المباراة (٢-٢) إلى سطر رمادي صغير بين قوسين.

```js
<span>${isPenScore ? penH : (d.homeScore ?? 0)}</span>   // ← المقلوب
```

من يمرّ سريعاً يقرأ أن الفريق فاز ٣-١، والمباراة في الحقيقة **تعادل**.
الترجيح وسيلة حسم لا نتيجة — ولذلك لا يدخل جدول الترتيب ولا جدول
الهدّافين. الآن: النتيجة الرسمية بالخطّ الكبير، والترجيح سطر ذهبي تحتها.

### ② مصادر متعدّدة للرقم نفسه

نتيجة الترجيح كانت تُقرأ من ثلاثة أماكن حسب الملف:

| الملف | المصدر |
|---|---|
| `cards-system.js` · `match-share-card.js` | `penaltyScoreHome` فقط |
| `viewer.js` | `_penScore` → الحقل ثم `liveData.penalties` |
| `admin.js` | `penalties` ثم `liveData.penalties` |

**النتيجة:** مباراة أُدخل ترجيحها من صفحة البثّ تُحفظ ركلاتها في
`liveData.penalties` — فتظهر عند الجمهور وتخرج **بطاقة المشاركة فيها
تعادل بلا فائز**.

الآن `PK.read(m)` قارئ واحد يجمع المصادر بترتيب مقصود:
**الركلات المفصّلة ← الرقم المجمّع ← أحداث المباراة المحفوظة**. المفصّلة
أولاً لأنها تحمل التسلسل والأسماء، والمجمّع شبكة أمان للبيانات القديمة.

### ③ العدّ لا يعرف قانون اللعبة

اللوحة القديمة كانت خانتين مستقلّتين، لكل فريق زرّا «هدف/تفويت» وصفّ
علامات. لا رقم للركلة، ولا «أفضل خمس»، ولا موت مفاجئ.

**وأخطر ما فيها:** لا شيء يمنع أن يُسجَّل لفريق **٧ ركلات** وللآخر **٣**
— وهو أكثر خطأ يقع فعلاً أثناء البثّ. ولا كشف للحسم الرياضي: ٣-٠ بعد
ثلاث ركلات لكلٍّ تعني **انتهت**، ومع ذلك كانت اللوحة تدعوك للمواصلة.

### ④ صياغتان وسطر يُطبع في غير موضعه

- صياغتان مختلفتان: `(ركلات الترجيح: 4 – 3)` في بطاقة النتيجة،
  و`(ركلات: 4 – 3)` في بطاقة التأهّل.
- ولا يذكر **من فاز بها**. و«٤-٣» وحدها تُقرأ معكوسة حسب ترتيب الفريقين
  على الشاشة.
- وفي `match-share-card.js` كان السطر يُطبع كلما وُجد الرقم — **حتى في
  مباراة حُسمت بنتيجتها (٣-١)**، فيظهر سطران متناقضان.

---

## الحلّ: `penalties-core.js`

### القارئ الواحد
```js
PK.read(m)    → { exists, h, a, kicks, source }
PK.verdict(m) → { officialText, decidedByPens, winnerName, text, short }
```
`verdict` هي الصياغة الوحيدة في المنصة كلها — بطاقة الجمهور، شجرة
الإقصاء، بطاقة النتيجة، بطاقة التأهّل، نصّ واتساب. فلا تختلف العبارة
بين شاشة وأخرى.

وشرطها دقيق: **لا تُعلن حسماً بالترجيح إلا إذا كانت المباراة تعادلاً
فعلاً وكان الترجيح غير متعادل.** فمباراة ٣-١ ومعها رقم ترجيح شاذ من
بيانات قديمة لا تُوصف بأنها حُسمت بالترجيح.

### محرّك القانون
```js
PK.state(kicks, first) → { h, a, phase, decided, winner, why, nextSide, nextNo }
```

| الحالة | السلوك |
|---|---|
| ٣-٠ بعد ٣ ركلات لكلٍّ | **محسومة** — «حُسمت قبل اكتمال الخمس» |
| ٣-١ بعد ٣ لكلٍّ | لم تُحسم — الخصم يستطيع اللحاق |
| ٤-٣ بعد الخمس | محسومة — «بعد الخمس ركلات» |
| ٤-٤ بعد الخمس | ⚡ الموت المفاجئ |
| ٥-٤ في الموت المفاجئ | محسومة — «بالموت المفاجئ» |
| ٥-٥ في الموت المفاجئ | مستمرّة |

**والتناوب صارم**: اللوحة تحسب دور من، وزرّا «سجّل/ضيّع» يسجّلان له وحده.
فلا يسبق فريق فريقاً أبداً. و«تراجع» يُزيل **آخر ركلة زمنياً** لا آخر
ركلة لأطول قائمة — الأولى تُعيد الحالة كما كانت، والثانية تخلط التسلسل.

### اللوحة الواحدة

يستعملها **البثّ والإدخال السريع** معاً (`_lpRenderPenDots` و
`_qrPenListHtml` كلاهما يستدعي `PK.board`)، فلا شكل ثانٍ يُتعلَّم:

- النتيجة الرسمية **ثابتة فوق اللوحة**: «نتيجة المباراة ٢-٢ · الترجيح
  لتحديد المتأهّل فقط».
- الطور معلن: `● أفضل خمس ركلات` أو `⚡ الموت المفاجئ`.
- صفّ لكل فريق: الركلات المسجَّلة، ثم الركلة القادمة نابضة برقمها، ثم
  خانات متقطّعة حتى الخمس — فيُرى كم بقي بلا حساب ذهني.
- «الدور على النصر — الركلة ٤» فوق زرّين اثنين لا أربعة.
- عند الحسم: تُقفل الأزرار وتظهر بطاقة «🏆 الهلال يتأهّل · بعد الخمس
  ركلات · ٤-٣».
- «بدأ:» يُضبط قبل أول ركلة ثم يُقفل كي لا ينقلب التسلسل.
- **التزامن**: الرقم المجمّع (`penaltyScoreHome/Away`) يتبع الركلات بعد
  كل ضغطة، فلا يتناقض المصدران أبداً.

### الجمهور: كرة الهدف نفسها وتحتها شارة

الخطّ الزمني كان يعرض دائرة مفرّغة فيها ✓ أو ✗ — شكل لا علاقة له بكرة
الهدف المرسومة فوقه بصفّين، فلا شيء يقول إن الصفّ ركلة.

صارت **نفس الكرة تماماً**: `Icon('ball', 12)` بنفس المقاس ونفس اللون
الذهبي المستعمل للهدف، وتحتها شارة دائرية صغيرة ✓ خضراء أو ✗ حمراء.
والركلة الضائعة تبهت قليلاً. تُقرأ بلمحة بلا نصّ.

---

## الاختبارات

| # | الفحص | النتيجة |
|---|---|---|
| ① | بداية فارغة | `home #1` ✓ |
| ② | ١-٠ بعد ركلة لكلٍّ | مستمرّة ✓ |
| ③ | ٣-٠ بعد ٣ لكلٍّ | **محسومة** — قبل اكتمال الخمس ✓ |
| ④ | ٣-١ بعد ٣ لكلٍّ | لم تُحسم ✓ |
| ⑤ | ٤-٣ بعد الخمس | محسومة — «بعد الخمس ركلات» ✓ |
| ⑥ | ٤-٤ بعد الخمس | تحوّلت للموت المفاجئ ✓ |
| ⑦ | ٥-٤ موت مفاجئ | محسومة ✓ |
| ⑧ | ٥-٥ موت مفاجئ | مستمرّة ✓ |
| ⑨ | الضيف يبدأ | التناوب انعكس صحيحاً ✓ |
| ⑩ | القارئ: ركلات مفصّلة | ✓ |
| ⑪ | القارئ: `liveData` بنصّ خام (`'goal'`) | ✓ |
| ⑫ | القارئ: الرقم المجمّع | ✓ |
| ⑬ | القارئ: أحداث المباراة المحفوظة | ✓ |
| ⑭ | مباراة بلا ترجيح | `exists:false` ✓ |
| ⑮ | فوز طبيعي ٣-١ مع رقم ترجيح شاذ | `decidedByPens:false` ✓ |

> **خطأ أُصلح أثناء الاختبار:** كان ترتيب الفحوص يصف الحسم بعد اكتمال
> الخمس بأنه «حُسمت قبل اكتمال الخمس» — وصف خاطئ يربك المنظّم. أُعيد
> ترتيب الشروط فصار كل حسم يحمل سببه الصحيح.

فحص صياغة `node --check`: `penalties-core.js` · `viewer.js` ·
`cards-system.js` · `match-share-card.js` · `admin.js` — وأُعيد فحص
وحدات v334–v336 الثلاث، صفر أخطاء.

## المعاينة

`معاينة-ركلات-الترجيح-v337.html` — خمسة مواضع **قبل وبعد** جنباً إلى
جنب: اللوحة · بطاقة الجمهور أثناء الترجيح · البطاقة المنتهية · الخطّ
الزمني · نصّ المشاركة. واللوحة في عمود «بعد» **حيّة** — اضغط «سجّل/ضيّع»
وشاهد التناوب والحسم والقفل.

## النشر

**ملف جديد: `penalties-core.js` — تأكّد من رفعه.**

| الملف | التغيير |
|---|---|
| `penalties-core.js` | 🆕 جديد |
| `admin.js` | 🔴 **الحفظ لم يعد يدهس النتيجة الرسمية** |
| `viewer.js` | النتيجة الرسمية أولاً + كرة الهدف للركلات (٤ مواضع) |
| `cards-system.js` | القارئ الموحّد + صياغة واحدة (٥ مواضع) |
| `match-share-card.js` | القارئ الموحّد + حذف السطر المكرّر (٤ مواضع) |
| `league-admin.html` · `league-viewer.html` | سطر التحميل |
| `sw.js` | `batolat-v337` + الملف الجديد |
