# v289 — إصلاحات شاملة بعد بلاغك

## ① 🚨 قسم «نظام المباريات» كان يُفتح فارغاً — خطئي

إعادة بنائي لفهرس الإعدادات في v287 **أسقطت معرّفَين** يعتمد عليهما الكود:
`setFormatRow` و`setTieRow`. ومع ذلك السبب الأعمق أن بطاقات الصفحة تظهر
بشرط نوع البطولة — وحين انقلب النوع أو غاب، لم تنطبق أي بطاقة فبدت الصفحة
فارغة تماماً كأنها معطّلة.

- أُعيد المعرّفان.
- **ولا تُترك الصفحة فارغة أبداً**: إن لم تنطبق أي بطاقة تظهر بطاقة بديلة
  تشرح السبب وتعرض طريق العودة. وإن كان النوع مفقوداً تقول صراحةً: «افتح
  منطقة الخطر ← استعادة نوع البطولة».

## ② نوع البطولة — تثبيت عند الإنشاء

إضافةً إلى إصلاح v288 (منع `saveSettings` من كتابة النوع)، صار المعالج
**يتحقّق بالقراءة بعد الكتابة**: النوع يُخزَّن في موضعين، وأي إخفاق صامت في
أحدهما يترك البطولة بنوع ناقص — وهي الحالة التي كانت تنقلب لاحقاً إلى
«دوري». يُعاد كتابة الموضع الناقص قبل أن يبدأ المنظّم العمل.

## ③ لا نصوص إنجليزية للمنظّم

رسائل مثل `x is not a function` كانت تصل كما هي. الآن:

- ترجمة لأخطاء جافاسكربت الشائعة برسائل تقول **ماذا يفعل** المنظّم.
- وأي نصّ يبقى لاتينياً بالكامل يُستبدل برسالة عربية عامّة، ويُسجَّل الأصل
  في الـConsole للتشخيص.
- **صفحتا الملحق مغلّفتان بحارس**: أي استثناء فيهما كان يترك القسم فارغاً
  أو يُظهر نصاً خاماً — الآن رسالة عربية وزرّ تحديث.

## ④ لوحة الفحص — التمرير والترتيب

كانت `flex + align-items:center` مع تمرير على الحاوية نفسها: تركيبٌ يقصّ
أعلى المحتوى الطويل ويُعلّق التمرير على الجوال، فلا يصل المنظّم لآخر البنود
ولا لزرّ الإغلاق.

**الآن:** رأس ثابت (فيه ملخّص وزرّ إغلاق دائم الظهور) · منطقة تمرير واحدة
للبنود مع `overscroll-behavior` · تذييل ثابت. وتمرير الصفحة خلف اللوحة
مقفل أثناء الفتح فلا يتنازع التمريران، ويُحرَّر عند الإغلاق.

## ⑤ ⚡ الإصلاح التلقائي — لكل بند زرّه

كل بند قابل للإصلاح صار له زرّ مستقلّ، **ولا يُنفَّذ شيء قبل نافذة تشرح
بالضبط ما سيحدث**: كم عنصراً، وماذا سيُحذف أو يُعدَّل، وما الذي لن يُمسّ.

| البند | الإصلاح التلقائي |
|---|---|
| مواجهة مكررة | يحذف الزائد وحده — **يُبقي ما له نتيجة**، وإلا الأقدم |
| مباريات فرق محذوفة | يحذفها (لا إصلاح آخر ممكن — أحد طرفيها غير موجود) |
| مباراة بين مجموعتين | **يحوّلها** إلى «مباراة فاصلة» — لا يحذف شيئاً |
| فريق مرتين في جولة | **ينقلها** لأول جولة يخلو فيها فريقاها — لا يحذف شيئاً |
| متأهلون غير معتمدين | يعتمد وينشر — لا يغيّر اختيارك |
| شجرة غير منشورة | يُظهرها للجمهور |

وزرّ **«إصلاح كل ما يمكن إصلاحه»** ينفّذها بموافقة واحدة تسرد البنود.
**وما يحتاج قراراً منك يبقى «إصلاح يدوي»** — التخمين هنا يفسد بطولة.
وبعد كل إصلاح يُعاد الفحص تلقائياً لترى الأثر.

## ⑥ بطاقات الإعدادات — مقاسات لا تقصّ محتواها

الصفوف كانت `flex` بلا `min-width:0` على النصّ، فالعناوين الطويلة تدفع
الشارة والسهم خارج البطاقة فيختفيان. طُبِّق على صفوف الإعدادات وصفوف
المفاتيح وبطاقات الأنواع، مع قصّ أنيق بالنقاط بدل الاختفاء.

## الاختبارات

بنية اللوحة الثلاثية · قفل التمرير وتحريره · حذف المكرر مع الإبقاء على ذات
النتيجة · حذف مباريات الفرق المحذوفة · اعتماد المتأهلين · **نقل المتزاحمة
بلا أي حذف (مُتحقَّق منه)** · وإعادة تشغيل **الاثنتي عشرة سلسلة** السابقة
كلها — صفر أخطاء.

## بعد الرفع

`batolat-v289` وكل المراجع `?v=289`. امسح بيانات الموقع مرة واحدة.
