# v272 — تطوير نافذة المباراة فوق أساس v217 العامل

الأساس هو مسار `addMatch` من v217 المؤكَّد عمله (بلا قفل للزر، وبلا غلاف
«حارس الحفظ المزدوج»). أُضيفت فوقه أربع طبقات، كلٌّ منها مختبَرة.

## ١) المباراة الفاصلة بين مجموعتين — عادت

- تُفتح من زرّها المخصّص في صفحة المجموعات فقط، وفقط إن كان إعداد
  «السماح بمباراة فاصلة بين مجموعتين» مفعّلاً — وإلا رسالة واضحة.
- عنوان النافذة يتغيّر إلى «⚔️ مباراة فاصلة بين مجموعتين»، ويظهر شريط
  ذهبي يشرح طبيعتها، و«رقم الجولة» يُخفى لأنه بلا معنى هنا.
- أسماء الفرق في القائمة تُلحَق بمجموعة كلٍّ منها، فيستحيل الاختيار بالغلط.
- التحقّق معكوس هنا: فريقان من **نفس** المجموعة يُرفضان برسالة صريحة.
- تُحفظ بـ`isKnockout: true` و`knockoutRoundName: "⚔️ فاصلة: A × B"` وبلا
  `groupId` — فتُعزل تماماً عن جداول ترتيب المجموعات، ويعرف الجمهور أنها
  مباراة قرار لا خطأ بالجدول.

## ٢) قائمة الفرق مقسّمة بالمجموعات

في نظام المجموعات تُقسَّم القائمتان بـ`<optgroup>` («المجموعة A»، «المجموعة
B» …)، والفرق غير الموزَّعة تُجمَع تحت «بلا مجموعة» بدل أن تختفي بين الباقين.
وأُضيف **خيار فارغ في المقدّمة**: كانت النافذة تفتح بفريقين لم يخترهما أحد
(أول اسمين في القائمة) وقد يُحفظان سهواً.

آلية الاختيار نفسها لم تتغيّر — ما زالت `<select>`، وهي المؤكَّد عملها.

## ٣) النافذة تبقى مفتوحة بعد الإضافة

- **التنبيه صريح ويذكر الفريقين**: «✓ تمت إضافة الأهلي × الهلال».
- وتحت النافذة **سجلّ الجلسة**: «✓ أُضيفت في هذه الجلسة (٣)» مع أسماء آخر
  المباريات — فلا يشكّ المنظّم هل حُفظت أم لا.
- يُصفَّر **الفريقان فقط**. التاريخ والوقت والملعب ورقم الجولة تبقى كما
  ضبطها المنظّم لأنها مشتركة بين مباريات الجولة الواحدة، فإدخال جولة كاملة
  صار سلسلة اختيارات بلا إعادة فتح في كل مرة.
- التصفير ضروري أيضاً حتى لا يوقظ الضغط التالي حوار «بينهما مباراة بالفعل»
  على نفس المواجهة.
- زر «إلغاء» صار «إغلاق» لأن النافذة لم تعد تُغلق تلقائياً.

## ٤) بطاقة المواجهة

فوق القائمتين بطاقة تعرض شعار كل فريق واسمه ومجموعته، وبينهما زرّ دائري
لتبديل الأرضية. الفرق بلا شعار تحصل على حرف أوّل ملوّن بدل فراغ. البطاقة
**عرضٌ فقط** يعكس ما في `<select>` — لم نجعلها وسيلة الاختيار حفاظاً على
الآلية العاملة.

## تحسينات صغيرة مصاحبة

- رقم الجولة يُقترح تلقائياً: أعلى جولة موجودة، أو التالية إن اكتملت.
- الملعب يُقترح من آخر ملعب استُعمل فعلاً، وإلا الملعب الافتراضي.
- التاريخ يُملأ باليوم إن كان فارغاً.
- بعد الحفظ يُفتح تبويب المباريات على الجولة الصحيحة تلقائياً.

## ما بقي من الإصلاحات السابقة

- `viewport-lock.js`: حارس منع الزوم ما عاد يستدعي `preventDefault` على
  `touchend` فوق العناصر التفاعلية — كان يُلغي حدث `click` نفسه.
- `addMatch` خارج قائمة «حارس الحفظ المزدوج» (لم يكن ملفوفاً في v217).
- مكافحة الكاش: `sw.js` بنسخة `batolat-v272` وكل المراجع `?v=272`.

## الاختبارات

دوري عادي · إضافتان متتاليتان بلا إعادة فتح · نظام مجموعات و`optgroup` ·
منع مواجهة بين مجموعتين في الوضع العادي · المباراة الفاصلة (مع الإعداد
مطفأً ومفعّلاً) · رفض فريقين من نفس المجموعة في وضع الفاصلة · تبديل
الأرضية · فريقان فارغان · فريق ضد نفسه.

## بعد الرفع

امسح بيانات الموقع مرة واحدة، أو `F12 ← Application ← Service Workers ←
Unregister` ثم `Ctrl+Shift+R`.
