# إصلاح: زر «إضافة المباراة» كان يعلّق ولا تُضاف المباراة

**الملف المعدَّل:** `admin.js` فقط. باقي ملفات الحزمة كما هي بلا أي تغيير.

## سبب المشكلة

في النسخة المطوّرة صارت `addMatch` تقفل الزر من أول ضغطة وتضعه على
«⏳ جارٍ الحفظ...» ولا تفتحه إلا بعد انتهاء العملية كاملة. وقبل الحفظ
توجد نداءات `confirmDialog` تنتظر ردّ المنظّم — فإن لم يصل ردّ، بقي
الزر مقفولاً ولا يقبل أي ضغطة بعدها.

وثلاثة أمور جعلت هذا يتكرّر:

1. **حواران متتاليان بنفس السؤال.** أُضيف حارس «⚠️ فريق يلعب مرتين في نفس
   الجولة»، ولم يُحذف الحارس القديم «⚠️ تعارض في الجولة» الذي يسأل نفس
   السؤال حرفياً. فيوافق المنظّم على الأول فيقفز ثانٍ مطابق — وعلى الجوال
   يبدو كأن الزر «رجع» ولم يحدث شيء.
2. **الإلغاء صامت تماماً.** الضغط خارج الحوار أو على «إلغاء» يُنهي الدالة
   بلا أي رسالة: لا المباراة تُضاف ولا سبب يظهر.
3. **لا يوجد `catch`.** أي خطأ قبل مرحلة الحفظ كان يخرج بصمت.

## ما تغيّر

- حُذف حوار «⚠️ تعارض في الجولة» المكرر. الفحص نفسه باقٍ في الحارس الأعلى
  برسالة أوضح — فحوار واحد فقط بدل اثنين.
- عند الإلغاء تظهر رسالة «أُلغيت إضافة المباراة» بدل الصمت.
- أُضيف `catch` يعرض الخطأ الحقيقي (مترجَماً عبر `_trErr`) بدل اختفائه.
- أُضيف حارس أمان (25 ثانية): إن تعلّق الحفظ لأي سبب — انقطاع شبكة أو
  حوار لم يُرَ — يُفتح الزر تلقائياً مع رسالة «تأخّر الحفظ — تأكّد من
  الاتصال وحاول مرة أخرى»، بدل الجمود الدائم.

## التحقّق

اختُبرت النافذة على سيناريوهات: دوري عادي، نظام مجموعات، إضافات متتالية،
جولة مكتملة، فريقان بينهما مباراة سابقة، وفريق يلعب مرتين في نفس الجولة.
في كل الحالات: حوار واحد كحدّ أقصى، والزر يعود لحالته الطبيعية دائماً.

## إن استمرّت المشكلة

افتح Console في المتصفح (F12) واضغط الزر، ثم أرسل أي خطأ أحمر يظهر —
فذلك يعني وجود سبب ثانٍ خارج هذا الملف.

---

# التحديث الثاني — السبب الحقيقي: حارس الزوم كان يبتلع الضغطات

العَرَض: الزر يُظهر أثر الضغط (يُحسّ أنه انضغط) ثم لا يحدث شيء إطلاقاً —
لا مباراة تُضاف، ولا تنبيه، ولا رسالة خطأ في الـConsole.

## السبب

في `viewport-lock.js` كان حارس منع «دبل-تاب زوم»:

```js
document.addEventListener('touchend', function (e) {
  if (Date.now() - lastTouchEnd <= 300) e.preventDefault();
  lastTouchEnd = Date.now();
}, { passive: false });
```

استدعاء `preventDefault()` على `touchend` يُلغي — بحكم مواصفة المتصفحات —
كل أحداث الفأرة المتولّدة عنه، ومنها `click` نفسه. فأي لمسة تقع خلال
٣٠٠ms من لمسة قبلها لا يصلها `click` أبداً: أثر الضغط يظهر (من
`touchstart`) ثم لا شيء، بلا أي خطأ.

لماذا ظهر الآن تحديداً: في النسخة القديمة كان اختيار الفرق عبر `<select>`
يفتح لوحة النظام، فيتباعد إيقاع اللمس. في النسخة المطوّرة صار الاختيار
أزراراً داخل النافذة، فيضغط المنظّم: الفريق الأول ← الثاني ← «إضافة
المباراة» بتتابع سريع — فتقع الضغطة الأخيرة داخل نافذة الـ٣٠٠ms وتضيع.

## ما تغيّر

- **`viewport-lock.js`** — الحارس يستثني الآن كل عنصر تفاعلي (`button`,
  `a`, `input`, `select`, `textarea`, `label`, `[onclick]`, `.btn`,
  `.tp-item` …). منع زوم الدبل-تاب باقٍ للمساحات الفارغة، ومضمون أصلاً عبر
  `touch-action:manipulation` المطبَّق في نفس الملف.
- **`league-admin.html`** — شبكة أمان: ربط `touchend` مباشرةً على زر
  «إضافة المباراة» كمسار بديل، مع مانع ازدواج زمني (٦٠٠ms) حتى لا تُضاف
  المباراة مرتين لو وصل الحدثان معاً.
- **`sw.js`** — رقم النسخة `batolat-v269`، وكل مراجع JS/CSS في صفحة
  الإدارة صارت `?v=269` — لضمان عدم خدمة أي ملف قديم من الكاش.
