# v275 — ⚡ الاختيار السريع + تمييز المجموعات

## ⚡ الاختيار السريع

اضغط فريقين: الأول مضيف، والثاني يُنشئ المباراة فوراً. لا نزول لقائمة
منسدلة، ولا فتح وإغلاق للنافذة بين مباراة وأخرى.

### قاعدة السلامة التي بُني عليها كل شيء

بعد سلسلة أعطال الحفظ السابقة، الشبكة صُمّمت على أربعة قيود صارمة:

1. **لا تلمس Firestore إطلاقاً** — تكتب في `matchHome`/`matchAway` ثم تنادي
   `addMatch()` نفسها. مسار حفظ واحد بلا ازدواج.
2. **لا تعطيل ولا قفل لأي عنصر** — القفل هو ما جعل الزر يبدو ميتاً سابقاً.
   المنع الوحيد للضغط المكرّر علمٌ يحمل وقته ويسقط تلقائياً بعد ٨ ثوانٍ.
3. **لا حوار تأكيد فوق النافذة** — الحوار غير المرئي كان أصل التعليق.
   الفحص يسبق الحفظ، فالحالات المشكوك فيها لا تصل إلى `addMatch()` أصلاً.
4. **مساران للأحداث** — `click` أساسي و`pointerup` احتياطي مؤجّل ٣٥٠ms
   يُلغى فور وصول `click`، لأن حُرّاس الزوم على الجوال تبتلع `click`.

### السلوك

- الضغطة الأولى → مضيف بشارة ذهبية · الثانية → المباراة تُنشأ فوراً.
- إعادة الضغط على المختار تلغي اختياره.
- **التحذيرات لا تُحفظ تلقائياً**: مواجهة مكررة أو فريق له مباراة في نفس
  الجولة → يُملأ الاختيار ويظهر شريط ذهبي، والقرار للمنظّم بزرّ الحفظ.
- **الموانع لا تملأ الخانة أصلاً** ورسالتها صريحة.
- سطر الخطوة أعلى الشبكة يقول ما المتوقَّع الآن، وبحث فوري بالاسم.

## 🎨 تمييز المجموعات

كان الفاصل بين المجموعات سطر نصّ ٩px بلون باهت — فتبدو المجموعتان كتلة
واحدة ولا يميّز المنظّم أين تنتهي إحداهما. الآن:

- كل مجموعة **قسم مؤطَّر بلونها**: حدّ جانبي عريض، خلفية بلمسة من اللون،
  شارة مربّعة بحرف المجموعة، واسمها بلونها، وعدّاد فرقها.
- الألوان من لوحة رسمية محدودة (ذهبي · أزرق · أخضر · بنفسجي · برتقالي ·
  فيروزي) وثابتة بترتيب المجموعة — فلا يتغيّر لون مجموعة بين فتحة وأخرى.
- عند اختيار المضيف، المجموعات التي لا تلتقي به **يخفت قسمها كاملاً**
  ويُكتب على رأسه «لا يلتقي مع المضيف» — سبب معلن بدل بهوت غامض.

## ↩︎ التراجع

كل سطر في سجلّ الجلسة معه زرّ «↩︎ تراجع» يحذف تلك المباراة فوراً — الإنشاء
بضغطتين يرفع احتمال الخطأ، والرجوع لصفحة المباريات مسار طويل.

## القائمة المنسدلة

لم تُحذف — صارت مطويّة تحت «اختيار من قائمة منسدلة» كبديل دائم يعمل
بالتوازي مع الشبكة على نفس الحقلين.

## إصلاح مصاحب

فتح النافذة كان يرث فريقَي آخر مباراة أُضيفت (لأن `populateMatchSelects`
تحافظ على القيمة السابقة عمداً أثناء إعادة الرسم) وقد يُحفظان سهواً. الفتح
صار يبدأ من صفحة نظيفة دائماً.

## بعد الرفع

`batolat-v275` وكل المراجع `?v=275`. امسح بيانات الموقع مرة واحدة.
