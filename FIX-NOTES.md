# إصلاح: تكرار/تفرّق أسماء الهدافين عند الجمهور

## المشكلة
لاعب يسجّل أهدافه عبر مصدرين مختلفين (صفحة البث المباشر / الإدخال السريع)
يظهر عند الجمهور كلاعبَين منفصلين، لأن أحد المصدرين لا يربط الهدف بـ playerId.

## التعديلات
1. **admin.js** (~سطر 2585): نافذة "الإدخال السريع" لتسجيل هدف أصبحت تستدعي
   `window._resolvePlayerId()` وتحفظ `teamId` / `playerId` / `playerNumber`
   مع الحدث — بنفس طريقة صفحة البث المباشر تماماً.

2. **league-viewer.html** و **league-admin.html** (داخل `ScorersCore.build`):
   أُضيفت تمريرة دمج ثانية (شبكة أمان) تدمج أي سطرين بنفس الفريق + الاسم
   المُطبَّع في سطر واحد — تعالج البيانات القديمة المسجّلة قبل هذا الإصلاح
   دون المساس بالفصل الصحيح بين لاعبين مختلفين بنفس الاسم من فريقين مختلفين.

## الاختبار
تم التحقق بسكربت Node.js مستقل:
- سيناريو الانقسام (نفس اللاعب، هدف بـ playerId + هدف بدونه) → يندمج في سطر واحد ✅
- سيناريو التصادم (لاعبان مختلفان بنفس الاسم من فريقين مختلفين) → يبقيان منفصلين ✅

## تحديث: خط زمن أحداث المباراة (الأهداف فقط)
- أُضيفت `_liveEventPlayerName()` في viewer.js: تعرض اسم الهدّاف الحالي من
  الكشف (rosterCache) عوضاً عن النص المجمَّد وقت تسجيل الهدف، مع رجوع آمن
  للاسم القديم لو الكشف غير محمَّل.
- أُضيفت `_ensureRosterLoaded()`: تحمّل كشف الفريقين تلقائياً عند فتح تفاصيل
  أي مباراة فيها أهداف مرتبطة بهوية، وتعيد رسم الشاشة فور اكتمال التحميل.
- طُبِّقت في مكانين: `openMatchDetail` (الخط الزمني الرئيسي) و
  `openBracketMatch` (تفاصيل مباريات خروج المغلوب).

## تنبيه هام — خارج نطاق هذا الإصلاح
البطاقات (يلو/أحمر)، التبديلات، والتشكيلات **لا تحمل أي معرّف هوية
(playerId) في أي مسار من مسارات الإدخال الحالية بالمنصة بأكملها**، ولا حتى
من صفحة البث المباشر. تحديث الاسم فيها بعد تعديله من الكشف يتطلب أولاً
بناء واجهة اختيار لاعب من الكشف (بدل خانة نص حرة) في 3 أماكن — هذه ميزة
جديدة، وليست إصلاح خلل، ولم تُنفَّذ هنا.

---

## v145 — ربط تعديل اسم اللاعب بكل الأماكن (بطاقات + تبديلات + تشكيلات)

**المشكلة سابقاً:** تعديل اسم لاعب في الكشف كان ينعكس على الهدّافين وخط زمن الأهداف فقط. البطاقات والتبديلات والتشكيلات كانت نصاً مجمّداً بلا هوية.

**ما تم:**
- **الإدخال (ربط الهوية عند الإنشاء):** كل مسارات إنشاء الأحداث صارت تخزّن هوية اللاعب:
  - البطاقات: `playerId`/`playerNumber` (البث المباشر + الإدخال السريع + بطاقات المباراة QR).
  - التبديلات: هوية الخارج (`playerOutId`) والداخل (`playerInId`) في المسارات الثلاثة.
  - التشكيلات: تخزين `id` لكل لاعب يُختار من الكشف (`admin-lineup-dragdrop.js`).
- **العرض (اسم حيّ من الكشف):**
  - الجمهور: البطاقات، البطاقة الثانية/الطرد، التبديلات (داخل/خارج)، والتشكيلة (أرضية الملعب + القائمة) تعرض الاسم الحيّ عبر `_liveEventPlayerName` / `_liveSubName` / `_lineupLiveName`.
  - الإدارة: قائمة أحداث البث + قائمة بطاقات/تبديلات المباراة تعرض الاسم الحيّ عبر `_adminLiveName`.
- **رجوع آمن:** أي حدث قديم بلا هوية يبقى باسمه المخزَّن دون كسر، وقبل تحميل الكشف يُعرض الاسم المخزَّن ثم يُحدَّث تلقائياً بعد التحميل.

**اختبارات معزولة:** ٨/٨ نجحت (بطاقة/تبديل داخل+خارج/تشكيلة تتحدّث؛ الأحداث القديمة تبقى؛ لا كسر قبل تحميل الكشف).

> ملاحظة: الربط يسري على الأحداث الجديدة المُدخلة عبر منتقي اللاعبين من الكشف. الأحداث القديمة المُدخلة كنص حر تبقى كما هي (لا هوية لها) — وهذا مقصود لتجنّب المطابقة الخاطئة بالاسم.

---

## v146 — إصلاح: اسم الهدّاف لا يتحدّث في الجمهور + خلط إحصائيات المتشابهين

**المشكلة الأولى:** تعديل اسم لاعب كان ينعكس في هدّافي الإدارة والبطاقات، لكن **ترتيب الهدّافين في الجمهور** يبقى بالاسم القديم.
- **السبب:** الجمهور كان يحمّل كشف الفريق مرة واحدة فقط (`getDocs`) ولا يعيد تحميله أبداً، وقسم الإحصائيات لم يكن يحمّل كشوف الفرق إطلاقاً — فلم يكن لدى محرّك الهدّافين كشفٌ يحلّ منه الاسم الحيّ.
- **الحل:**
  1. `_ensureRosterLoaded` صار مستمعاً حيّاً (`onSnapshot`) بدل تحميل لمرة واحدة → أي تعديل اسم ينعكس فوراً ويعيد رسم الهدّافين/البطاقات/تفاصيل المباراة.
  2. `renderStats` يضمن تحميل كشوف كل الفرق عند فتح الإحصائيات ويعيد الرسم عند وصولها.
  3. `_collectScorerRosters` يدمج أيضاً اللاعبين من `teams[]` المُزامَنة حيّاً (الاسم الحيّ له الأولوية حسب الهوية).

**المشكلة الثانية:** الضغط على لاعب اسمه «سلطان» من البطاقات قد يعرض إحصائيات «سلطان» آخر بنفس الاسم.
- **السبب:** نافذة اللاعب كانت تبحث وتجمع الإحصائيات بالاسم، فيختلط لاعبان بنفس الاسم في نفس الفريق.
- **الحل:** `openPlayerModal` صار يستقبل `playerId` ويفصل به تماماً (اختيار اللاعب + عدّ الأهداف/البطاقات)، مع رجوع آمن للاسم في البيانات القديمة بلا هوية. صفوف الإحصائيات تمرّر `playerId`.

**اختبارات معزولة:** كلها نجحت — تحديث اسم الهدّاف عبر الهوية، فصل لاعبين بنفس الاسم/الفريق في العدّ، ودمج آمن للبيانات القديمة.

---

## v147 — تعميم التحديث الحيّ للاسم فور تعديله من إدارة الفريق

**التأكيد:** تعديل اسم لاعب من «إضافة/تعديل لاعبين» في الفريق يُحفَظ في `teams/{teamId}/roster/{playerId}` — وهو المصدر نفسه الذي تقرأ منه كل الأقسام عبر هوية اللاعب.

**تحسين هذا الإصدار:** كانت مستمعات الكشف الحيّة في الجمهور تبدأ فقط عند فتح تبويب الإحصائيات أو تفاصيل مباراة. الآن تبدأ لكل الفرق **فور تحميل قائمة الفرق** — فينعكس تعديل الاسم مباشرةً في: جدول الهدّافين، أقسام البطاقات، خط أحداث المباراة، التشكيلات، والقائمة المصغّرة في الرئيسية — دون أن يحتاج المشاهد لفتح أي شيء. أُضيف تجميع (debounce) لإعادة الرسم لتفادي الوميض.

**الجانب الإداري:** كشف الفريق في الإدارة مستمع حيّ أصلاً ويعيد رسم الهدّافين فور الحفظ.

**شرط التحديث اللحظي:** أن يحمل الحدث هوية اللاعب (`playerId`) — وهو الحاصل لكل حدث أُدخل عبر منتقي اللاعبين. الأحداث القديمة جداً (نص حر بلا هوية) التي تغيّر اسمها ولم يعد مطابقاً للكشف تبقى باسمها حتى يُعاد إدخالها مرة من المنتقي.

---

## v148 — إعادة بناء نظام أسماء اللاعبين (نظيف + بلا خبص)

**الأعراض:** تعديل اسم لاعب كان أحياناً لا ينعكس، أو يعرض اسماً قديماً، أو يدمج لاعبين مختلفين.

**الأسباب الجذرية التي أُزيلت:**
1. **مصادر متعارضة:** كان `_collectScorerRosters` يدمج `teams[].players` (حقل قديم لا يُحدَّث) ويمنحه الأولوية، فيطغى الاسم القديم على الكشف الحيّ. → أُزيل نهائياً؛ الكشف الحيّ (subcollection) هو المصدر الوحيد الموثوق، والتشكيلات تُكمّل فقط اللاعبين غير الموجودين دون تعديل أي اسم له هوية.
2. **مستمعات متعددة/تحميل لمرة واحدة:** توحّدت في مستمع حيّ واحد لكل فريق (`_ensureRosterLoaded` + `onSnapshot`)، يبدأ لكل الفرق فور تحميل قائمة الفرق، ويعيد الرسم بتجميع (debounce) للهدّافين/البطاقات/الأحداث/التشكيلات/صفحة الفريق.
3. **مُحلّل موحّد:** كل نقاط العرض تستخدم `_pName(teamId, playerId, fallback)` — لا منطق مكرر.
4. **دمج خاطئ للمتشابهين:** «شبكة الأمان» كانت تدمج أي سطرين بنفس الاسم/الفريق حتى لو كانا لاعبَين مختلفَين بهويتين. → صارت لا تدمج إطلاقاً سطرين لهما هويتان مختلفتان (في الهدّافين والبطاقات).

**النتيجة:** بمجرد الضغط على «حفظ» في تعديل اسم اللاعب، ينعكس الاسم فوراً في كل مكان، دون خبص، ودون دمج لاعبين مختلفين.

**اختبارات:** اسم محدّث ينعكس؛ لاعبان بنفس الاسم/الفريق يبقيان منفصلين؛ دمج آمن لهوية+بلا‑هوية؛ نفس المنطق للبطاقات — كلها نجحت. وكل الملفات والسكربتات المضمّنة تمرّ فحص الصياغة.

---

## v149 — الهدّافون في الإدارة: عرض الكل + زر تعديل الاسم المختصر

- **عرض الكل:** أُزيل حدّ «أفضل ٢٠» من جدول الهدّافين في الإدارة؛ يُعرض كل اللاعبين الذين سجّلوا، مع رأس يبيّن الإجمالي.
- **زر التعديل المختصر:** كل صفّ هدّاف فيه زر ✏️ يفتح كشف الفريق ويدخل مباشرةً في وضع تعديل اسم/رقم/مركز ذلك اللاعب (`scorerEditPlayer` → `openRosterModal` → `editRosterPlayer`). حُسِّن ليعمل فوراً إن كان الكشف جاهزاً، وإلا ينتظر وصوله بأمان، مع تمرير تلقائي للصفّ وتحديد النص في خانة الاسم.
- بعد الحفظ يكتب `saveRosterEdit` في الكشف، ومستمع الكشف الحيّ يعيد رسم الهدّافين فوراً بالاسم الجديد — ومنه ينعكس في كل مكان.

---

## v150 — بحث اللاعبين داخل الإحصائيات (تجربة الجمهور)

- أُضيف صندوق بحث أعلى تبويب الإحصائيات في الجمهور، يفلتر الأقسام الأربعة (هدّافون · صنّاع · صفراء · حمراء) بالاسم لحظياً.
- أثناء البحث تُعرض كل المطابقات (يتجاوز حدّ الـ٥/عرض المزيد)، ويظهر «لا يوجد لاعب بهذا الاسم» في القسم الفارغ، ويُخفى ملخص البطولة لإبراز النتائج.
- يستخدم نفس تنسيق بحث المباريات ومطابقة اسم مرنة (تتجاهل التشكيل والمسافات). زر ✕ يمسح البحث.

---

## v151 — إحصائيات الإدارة الموحّدة + الصنّاع في نافذة اللاعب

**قسم الإحصائيات في الإدارة (بدل «الهدّافون»):**
- أصبح قسماً موحّداً: هدّافون · صنّاع · بطاقات صفراء · بطاقات حمراء — كلٌّ يُبنى بالهوية (playerId) مع الاسم الحيّ من الكشف.
- زر تعديل ✏️ على كل لاعب في كل الأقسام (يفتح كشف الفريق مباشرة على وضع تعديل الاسم — نفس `scorerEditPlayer`).
- قسم الصنّاع يظهر فقط عند تفعيله من الإعدادات.
- **دُمج مولّد بطاقات المشاركة** (cards-system) داخل نفس الصفحة في حاوية `cs-host`؛ حُذف قسم «البطاقات» المنفصل من الشريط الجانبي و`showPage` صار يبني المولّد عند فتح صفحة الإحصائيات.

**نافذة إحصائيات اللاعب (الجمهور):**
- أُضيفت خانة «👟 صناعة» (تظهر عند تفعيل الصنّاع أو إن كان للاعب صناعات)، مع شارة صناعة في سجل المباريات. تُحتسب الصناعة بالهوية إن توفّرت وإلا بالاسم.

**اختبارات:** محرّك إحصائيات الإدارة (هدّافون/صنّاع/صفراء/حمراء بالهوية + الاسم الحيّ) وعدّ الصناعات في النافذة — كلها نجحت. جميع الملفات تمرّ فحص الصياغة.

---

## v152 — فيديو المباراة المنتهية (يظهر مكان البث)

**الوضع:** المنصة تدعم الفيديو أصلاً (نظام `_buildVideoEmbed`/`_toEmbedUrl` — يوتيوب/تويتش iframe، فيسبوك/تيك‑توك زر، mp4 مشغّل). لكن كان يُقرأ حقل `videoUrl` فقط، بينما نافذة «معلومات المباراة» (mcv2) تحفظ `streamUrl` — فلم يكن رابط المباراة المنتهية يظهر.

**الإصلاح (خفيف جداً — رابط نصّي فقط، بلا رفع فيديو):**
- نافذة «معلومات المباراة» (متاحة لكل مباراة منتهية عبر زر «معلومات المباراة») صارت تحفظ الرابط في `videoUrl` أيضاً، فيظهر للجمهور تلقائياً مكان البث أعلى صفحة المباراة، بشارة «📼 إعادة».
- الحقل يُعبّأ مسبقاً من `videoUrl`، وحُسّنت تسميته وتلميحه ليوضّح أنه يظهر مكان البث.
- لا ثقل على المنصة: يُخزَّن نص الرابط فقط، والفيديو يُستضاف على يوتيوب/تويتش ويُحمَّل فقط عند فتح صفحة تلك المباراة (lazy).

**اختبار:** تحويل الروابط (يوتيوب/تويتش/mp4/فيسبوك) سليم؛ الملفات تمرّ فحص الصياغة.

---

## v153 — دعم كل أشكال روابط يوتيوب للتضمين المباشر

توسيع `_toEmbedUrl` ليضمّن داخل الصفحة أيضاً: روابط `youtube.com/shorts/...` و`youtube.com/embed/...` الجاهزة — إضافةً للأشكال المدعومة سابقاً (watch, youtu.be, live, m.youtube). النتيجة: أي رابط يوتيوب يضعه المنظّم يُشغَّل داخل صفحة المباراة مباشرة دون نقل المشاهد لأي منصة. (فيسبوك/تيك‑توك تبقى زرّاً لأنها تمنع التضمين من طرفها.)

---

## v155 — إصلاح امتلاء المساحة: إيقاف تكرار شعارات base64 في المباريات (بحذر)

**السبب الجذري لامتلاء الجيجا:** شعار كل فريق (base64 ~28KB) كان يُنسخ داخل كل مستند مباراة (homeLogo/awayLogo). في دوري كبير = مئات النسخ المكرّرة → تضخّم قاعدة البيانات ورسالة «تجاوزت 1 جيجا».

**الإصلاح (آمن — العرض يعتمد أصلاً على شعار الفريق):**
- تأكيد أن كل قراءات الشعار في المنصة (جمهور + إدارة + مولّدات الصور) تتبع النمط: `teams.find(id) || {logo: m.homeLogo}` — أي تجلب الشعار من الفريق، والمخزّن احتياطي فقط.
- دالة مركزية `_lightMatch()` تُفرّغ شعارات base64 الثقيلة قبل كتابة أي مباراة جديدة (الإيموجي وروابط http تبقى). طُبّقت على كل مواقع إنشاء المباريات (دوري/مجموعات/إقصاء/جدولة تلقائية + شجرة knockoutRounds المضمّنة) — 11 موضعاً.
- **لا تمسّ المباريات القديمة المحفوظة.**

**أداة تنظيف اختيارية (منطقة الخطر → «تنظيف الشعارات»):** تُفرّغ base64 من المباريات القديمة لتحرير المساحة، على دفعات (400/دفعة)، ولا تلمس إلا مباراة لها homeId/awayId صحيح، ولا تغيّر أي حقل آخر. يشغّلها المنظّم يدوياً.

**اختبار:** `_lightMatch` (base64 يُفرّغ، إيموجي/http يبقى، قيم فارغة آمنة) — نجح. الملفات تمرّ فحص الصياغة. قراءات الشعار في الجمهور لم تتغيّر.

---

## v156 — صور اللاعبين في كل مكان + ضغط قوي

**العرض (بشكل التطبيقات الرسمية):**
- الهدّافون + الصنّاع + الكروت (جمهور وإدارة): صورة اللاعب دائرية أنيقة مع شارة صغيرة لشعار الفريق في الزاوية. لو لا توجد صورة → شعار الفريق كالسابق (بلا تغيير للمظهر القديم).
- نافذة إحصائيات اللاعب: صورته أعلى النافذة (سبق).
- التشكيلات: صورة اللاعب في القائمة وعلى أرضية الملعب (مع شارة رقم صغيرة) — نمط رسمي.
- كل الصور تُجلب بالهوية (playerId) من الكشف الحيّ، وتُحدّث تلقائياً.

**الضغط القوي (`_compressPlayerPhoto`):**
- قصّ مربّع من المنتصف (تأطير أفضل للوجه) بأبعاد 256px.
- WebP بجودة متناقصة ثم JPEG، بحدّ أقصى صارم ~45KB.
- لو بقيت أكبر (نادر) يصغّر البُعد تلقائياً لـ160px ويعيد. النتيجة صغيرة جداً مهما كبر الأصل (حتى صور 10MB).

**اختبار:** محلّلات الصور (جلب بالهوية، رجوع آمن للشعار) نجحت؛ الملفات تمرّ فحص الصياغة.

---

## v157 — إصلاح: رجوع مباراة الجولة الثانية إلى الجولة الأولى عند البث

**العرَض:** عند إضافة مباريات الجولة الثانية وبدء البث، ترجع المباراة إلى الجولة الأولى وتُحتسب/تُعرض عليها.

**السبب الجذري:** صفحتا حفظ البث (`_lpSaveV2` وحفظ البث الأقدم) كانتا تكتبان `round` من حقل `lp-round` بصيغة `parseInt(value || 1)`. لو كان الحقل غير محمّل أو فارغاً وقت الحفظ (يحدث عند فتح البث قبل اكتمال تحميل بيانات المباراة)، تصبح القيمة **1** فتُكتب فوق جولة المباراة الحقيقية (2) — فترجع للجولة الأولى.

**الإصلاح:** لا يُكتب `round` إلا إذا أدخل المنظّم قيمة صحيحة (>=1) صراحةً؛ وإلا تبقى جولة المباراة الأصلية كما هي (تُقرأ من `_liveMatches`/`matches`). طُبّق على مساري حفظ البث. (نافذة «معلومات المباراة» كانت آمنة أصلاً.)

**اختبار:** منطق الحفاظ على الجولة — حقل مفقود/فارغ يُبقي الجولة، إدخال صريح يُطبَّق، قيمة غير صالحة تُتجاهَل — كلها نجحت. الملفات تمرّ فحص الصياغة.

---

## v158 — إصلاح: الأسماء المعدّلة (اسم واحد ← اسمين) لا تتحدّث في الهدّافين/البطاقات

**السبب:** الأحداث القديمة بلا هوية (playerId) كانت تُربط باللاعب فقط بتطابق الاسم التام. عند تعديل «محمد» ← «محمد العلي» في الكشف، لم يعد الاسم القديم يطابق الجديد، فبقيت الأحداث بالاسم القديم.

**الإصلاح (جزآن):**
1. **مطابقة بادئة آمنة** في `lookupRoster` (جمهور) و`_adminBuildStat` (إدارة): حدث «محمد» يُربط بـ«محمد العلي» إن كان التطابق وحيداً. آمن: عند وجود لاعبَين بنفس البادئة لا يخمّن، ولا يطابق «محمد» بـ«محمود» (حدود كلمة).
2. **ربط دائم عند التعديل:** `saveRosterEdit` يضيف الآن `playerId` للأحداث القديمة المطابقة للاسم القديم/الجديد (على دفعات، مباريات هذا الفريق فقط، بلا لمس أي حقل آخر) — فلا تعود المشكلة.

**اختبار:** مطابقة الأسماء (بادئة/تام/التباس/محمد≠محمود) + سيناريو التعديل الكامل في الهدّافين والبطاقات — كلها نجحت. الملفات تمرّ فحص الصياغة.

---

## v159 — فصل مولّد صور المشاركة + عرض 5/المزيد في إحصائيات الإدارة

- **فصل «بطاقات المشاركة»:** أُعيد مولّد صور المشاركة إلى صفحة مستقلة (`page-cards`) بعنصر جانبي خاص، وأُزيل من داخل صفحة الإحصائيات. حاوية `cs-host` انتقلت للصفحة المستقلة، وهوك cards-system عاد يعمل على صفحة `cards` فقط.
- **عرض 5 + «عرض المزيد»:** أقسام إحصائيات الإدارة (هدّافون/صنّاع/صفراء/حمراء) تعرض أول 5 مع زر «عرض المزيد (N)» يوسّع/يطوي — تماماً مثل الجمهور. مع عدّاد إجمالي لكل قسم.

الملفات تمرّ فحص الصياغة.

---

## v160 — إعادة ترتيب أزرار البث (تخطيط رسمي نظيف)

- **تخطيط شبكي منظّم:** أزرار التحكم بالوقت صارت شبكة عمودين متساوية بدل صفّ متعرّج. الأزرار الأساسية (بدء المباراة / إنهاء الشوط / إنهاء المباراة) تمتدّ على العرض الكامل لإبرازها، والثانوية (إيقاف مؤقت / بدل الضائع / وقت إضافي / ترجيح) في صفّ مرتّب فوقها. أحجام موحّدة وارتفاع ثابت.
- **بلا زخارف/أنيميشن:** أُزيل نبض زر الاستئناف والحركات الزائدة. ألوان أوضح: بدء=أخضر ممتلئ، إنهاء=أحمر ممتلئ.
- **حالة المباراة أوضح:** شارة الحالة والفترة (الشوط) أنظف وأبرز.

الملفات تمرّ فحص الصياغة.

---

## v161 — تعديل صاحب الهدف من منتقي لاعبي الفريق (المباريات المنتهية)

في نافذة «تعديل الحدث» (من زر تعديل الأحداث في المباراة المنتهية)، للأهداف/ركلات الجزاء/البطاقات:
- يظهر الآن **منتقي بكل لاعبي الفريق** (يُحمّل كشف الفريق) — تختار صاحب الهدف الجديد بضغطة، أو تكتب اسماً يدوياً.
- عند اختيار لاعب، يُربط الحدث **بهويته (playerId)** فينتقل الهدف تلقائياً: يُخصم من اللاعب القديم ويُضاف للجديد في الهدّافين والإحصائيات، مع تحديث رقم اللاعب من الكشف.
- الكتابة اليدوية لاسم مختلف تفكّ الربط (سلوك سابق محفوظ). التبديل والهدف العكسي بلا تغيير.

**اختبار:** نقل الهدف بالهوية (خصم من القديم، إضافة للجديد، وحالة هدفين نُقل أحدهما) — كلها نجحت. admin.js يمرّ فحص الصياغة.

---

## v162 — إصلاح: هدّافو الفريق + أصفار نافذة اللاعب + رفض/ضغط الصور

**1) هدّافو الفريق (صفحة الفريق بالجمهور):** كانوا يُبنون من نص `homeScorers` (يعرض الدقائق ويخبص). الآن يُبنون من نفس محرّك الهدّافين بالهوية (`buildScorersData`) — نظيف كجدول الهدّافين، ويمرّر playerId للنافذة.

**2) أصفار إحصائيات اللاعب:** كانت النافذة تَعُدّ الأحداث بالهوية فقط عند وجود playerId للاعب، فالأحداث القديمة (بلا هوية) لا تُحتسب → أصفار. الآن: مطابقة بالهوية عند توفّرها في الطرفين، وإلا بالاسم — فتُحتسب القديمة والجديدة. أُضيف عدّ «مباريات لعبها» الفعلي من التشكيلة.

**3) رفع صور اللاعبين:**
- أُزيل رفض الحجم قبل الرفع (كان يرفض حتى صور 70KB برسالة مضلّلة). الضغط يتكفّل بأي حجم.
- الآن: «جارِ الضغط (الحجم الأصلي)» ← «جارِ الرفع، الحجم بعد الضغط XKB (وفّرنا N%)» ← يُرفع للـStorage ويُحفظ الرابط فقط.
- رسائل خطأ دقيقة: تفرّق بين قواعد Storage غير المنشورة / انتهاء الجلسة / مشكلة الاتصال — بدل «الحجم كبير» الخاطئة.
- المساحة تُحسب بعد الضغط (الرابط فقط في Firestore، والصورة في Storage).

**اختبار:** مطابقة النافذة (هوية/اسم، قديم/جديد) ونقل هدّافي الفريق للمحرّك الموحّد — نجحت. الملفات تمرّ فحص الصياغة.

---

## v163 — إزالة «رجل المباراة» من نافذة إحصائيات اللاعب

أُزيل بالكامل (كان لا يعمل ويسبب خبطاً): خانة «⭐︎ رجل» من رأس النافذة، شارة التاج 👑 من صفوف المباريات، حساب momCount، وشرط عرض المباراة بسبب MOM. باقي الإحصائيات (أهداف/صناعة/مباريات/معدل/كروت) كما هي. viewer.js يمرّ فحص الصياغة.

---

## v164 — رفع صور اللاعبين عبر Cloudinary (بدون بطاقة، بدل Firebase Storage)

بُدّل مزوّد تخزين الصور من Firebase Storage (الذي تطلّب Blaze + CNTXT في السعودية) إلى **Cloudinary** (خطة مجانية دائمة بلا بطاقة):
- إعدادات: `CLOUDINARY_CLOUD='ddubylfs'`, `CLOUDINARY_PRESET='wvebrqwq'` (unsigned).
- `uploadRosterPhoto` الآن: يضغط الصورة (256px، ~45KB)، يرفعها لـ Cloudinary عبر `_uploadToCloudinary` (fetch → api.cloudinary.com، preset غير موقّع)، ويحفظ `secure_url` فقط في مستند اللاعب.
- يعرض الحجم قبل/بعد الضغط ونسبة التوفير. رسائل خطأ دقيقة (preset/اتصال).
- العرض بلا تغيير: كل نقاط عرض الصورة تقرأ حقل `photo` (رابط) — تعمل مع أي مزوّد.
- `removeRosterPhoto` يمسح الرابط من اللاعب. لا استهلاك من حصة Firestore (رابط نصّي فقط؛ الصور في Cloudinary، حصته ~25 credit/شهر ≈ 5GB).

admin.js و viewer.js يمرّان فحص الصياغة.

---

## v165 — ترتيب خانات إحصائيات اللاعب بعد حذف «رجل المباراة»

بعد إزالة رجل المباراة، كانت الشبكة مثبّتة على 4 أعمدة فيبقى مكان فارغ عند إخفاء خانة الصناعة (3 خانات في شبكة 4). الحل: تحويل `.pm-grid` إلى flex متساوٍ (`flex:1 1 0`) يتكيّف تلقائياً مع عدد الخانات الظاهرة (3 أو 4) — بلا فراغ، وتوزيع متساوٍ على العرض. viewer.css فقط.

---

## v166 — إظهار صور اللاعبين في التشكيلات (حتى المنتهية) وقائمة لاعبي الفريق

**السبب:** التشكيلة تُحفظ بالاسم/الرقم فقط بلا `id`، فكان `_lineupPhoto(p.id)` يفشل دائماً. وقائمة اللاعبين في صفحة الفريق لم تكن تعرض صورة أصلاً.

**الحل:**
- `_lineupPhoto` صار يقبل كائن اللاعب ويطابق: بالهوية إن وُجدت، وإلا **بالاسم + الرقم**، وإلا بالاسم — فتظهر الصور في التشكيلات (أرض الملعب + القائمة) لكل المباريات بما فيها المنتهية.
- قائمة اللاعبين في صفحة الفريق: أُضيفت صورة دائرية لكل لاعب (من `p.photo` مباشرة) مع رقمه بجانب اسمه.

**اختبار:** مطابقة صورة التشكيلة (بالاسم/الرقم بلا id، بالاسم فقط، بالهوية، بلا صورة، اسم غير موجود) — كلها نجحت. viewer.js يمرّ فحص الصياغة.

---

## v167 — صور اللاعبين في قسم البطاقات (والأحداث القديمة بلا هوية)

كانت `_playerPhoto` (جمهور) و`_adminStatRow` (إدارة) تبحث عن الصورة بالهوية فقط، فأحداث البطاقات القديمة بلا playerId لا تظهر صورها. الآن كلاهما يطابق بالهوية إن وُجدت، وإلا **بالاسم** — فتظهر صور اللاعبين في البطاقات الصفراء/الحمراء (وكل الأقسام) في الجمهور والإدارة.

اختبار: مطابقة الصورة بالاسم بلا هوية + بالهوية + بلا صورة — نجحت. الملفات تمرّ فحص الصياغة.

---

## v168 — صور اللاعبين للاحتياط/التبديلات

لاعبو الاحتياط (البدلاء) في قائمة التشكيلة كانوا يعرضون صورهم أصلاً (v166). أُضيفت الآن الصور في قسم «التبديلات» (renderSubsSection): صورة دائرية صغيرة للاعب الداخل والخارج (مطابقة بالاسم)، مع رجوع لرمز التبديل ▲/▼ إن لا صورة. أُضيف نمط `.sub-av`. viewer.js يمرّ فحص الصياغة.

---

## v169 — تطوير التشكيلة والبدلاء وقائمة لاعبي الفريق

**الملعب:** خلفية عشب متدرّجة بخطوط جزّ + ظل داخلي (شكل احترافي). أفاتار اللاعب أكبر (40px) مع ظل، واسم أوضح. **كل لاعب على الملعب قابل للضغط** → يفتح نافذة إحصائياته (بالاسم/الهوية + teamId).

**مقاعد البدلاء:** بدل قائمة تحت بعض → **شبكة 3 في الصف** (bench-grid): بطاقة أنيقة لكل بديل فيها صورته الدائرية (48px) + اسمه + رقمه، قابلة للضغط لفتح إحصائياته. عدّاد للبدلاء.

**قائمة التشكيلة:** كل صف صار قابلاً للضغط (يفتح الإحصائيات) مع تمييز عند اللمس.

**قائمة لاعبي الفريق (صفحة الفريق):** صور دائرية 38px + قابلة للضغط لفتح إحصائيات اللاعب.

**نافذة اللاعب:** صورة الرأس تُطابق بالهوية أو **بالاسم** (تظهر حتى للاعبين المفتوحين بالاسم من التشكيلة).

تمرير teamId إلى renderPlayersOnPitch/renderLineupList لجلب الصور بدقة وفتح النوافذ. viewer.js يمرّ فحص الصياغة.

---

## v170 — ظلّ لاعب (silhouette) بدل الرقم عند غياب الصورة

عند عدم وجود صورة للاعب، يظهر الآن **ظلّ لاعب أنيق (SVG silhouette)** داخل دائرة نظيفة + رقمه بشارة صغيرة — كالتطبيقات الرسمية — في الملعب والبدلاء. دالة `_playerSilhouetteSVG`. viewer.js يمرّ فحص الصياغة.

---

## v171 — تسهيلات تعبئة اللاعبين (4 ميزات)

في نافذة «قائمة اللاعبين» بالإدارة، أُضيفت أدوات فوق نموذج الإضافة اليدوية (بلا مساس بها):
1. **📋 لصق دفعة أسماء:** نافذة لصق — اسم بكل سطر، دفعة واحدة. مُحلّل ذكي `_parsePlayerLine` يفصل الرقم عن الاسم: «10 محمد» / «محمد 10» / «محمد» / «1 - فهد» / «سلطان (حارس)». يتخطّى المكرّر بالاسم. حفظ على دفعات.
2. **إدخال سريع (رقم+اسم بالسطر):** مغطّى بنفس المُحلّل ضمن اللصق الدفعي.
3. **👥 نسخ من فريق آخر:** يختار فريقاً مصدراً وينسخ لاعبيه (اسم/رقم/مركز — لا الصورة)، متخطّياً المكرّر.
4. **⚙️ تفاصيل اختيارية للاعب:** في نموذج التعديل، قسم قابل للطي: العمر/الجنسية/الطول/القدم المفضّلة — كلها اختيارية. تُعرض في نافذة اللاعب بالجمهور تحت اسم الفريق إن مُلئت.

اختبار: مُحلّل الأسطر (كل الصيغ) نجح. admin.js و viewer.js يمرّان فحص الصياغة.

---

## v172 — قالب اللاعبين + استيراد من ملف (Excel/CSV) — بديل نظيف

حُذفت أدوات v171 (لصق دفعة / نسخ من فريق) بناءً على طلب المستخدم، واستُبدلت بنظام ملف:
- **الإعدادات → «📄 قالب اللاعبين»:** زرّان لتنزيل قالب فارغ (Excel أو CSV) بكل الأعمدة: الرقم/الاسم/المركز/الحالة/العمر/الجنسية/الطول/القدم. مع صفّي مثال توضيحيين.
- **نافذة قائمة اللاعبين → «📥 استيراد»:** يرفع الملف (xlsx/csv)، يقرأه، يحوّل المراكز/الحالات العربية لمفاتيح داخلية، ويستورد على دفعات متخطّياً المكرّر بالاسم.
- أُضيفت مكتبة SheetJS (CDN) لملفات Excel. مُحلّل CSV داخلي يدعم الاقتباس والفواص1 داخل الحقول + BOM للعربي.

اختبار: قراءة CSV (اقتباس/فواصل)، تحويل المركز/الحالة، وإنشاء/قراءة القالب — نجحت. admin.js و viewer.js يمرّان فحص الصياغة.

---

## v173 — عرض التفاصيل الاختيارية للاعب كبطاقات مرتّبة

كانت التفاصيل الاختيارية تظهر كسطر واحد مزدحم تحت اسم الفريق. الآن قسم مستقل `pmDetails` يعرضها **بطاقات مرتّبة** (شبكة عمودين): الرقم 👕 · المركز 🎯 · العمر 🎂 · الجنسية 🌍 · الطول 📏 · القدم 🦶 — كل تفصيل ببطاقة فيها رمز + تسمية + قيمة. تظهر فقط التفاصيل المُدخلة (اختيارية بالكامل)؛ إن لم يُدخل شيء لا يظهر القسم. viewer.js يمرّ فحص الصياغة.

---

## v174 — أيقونات SVG بدل الإيموجي في بطاقات تفاصيل اللاعب

استُبدلت إيموجي بطاقات التفاصيل (الرقم/المركز/العمر/الجنسية/الطول/القدم) بأيقونات SVG أنيقة موحّدة اللون (ذهبي، تتبع currentColor). كل أيقونة مرسومة بخطوط نظيفة. `.pm-detail-ic svg{17px}`. viewer.js يمرّ فحص الصياغة.
