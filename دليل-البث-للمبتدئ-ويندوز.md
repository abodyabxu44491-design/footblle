# 🎓 دليلك الكامل: إعداد خادم البث من الصفر (ويندوز + مبتدئ)

> اتبع هذا حرفياً، خطوة خطوة. لا تتخطّى أي خطوة. الوقت المتوقّع: ساعة إلى ساعتين (مرة واحدة في حياتك).
> بعد ما تخلّص، كل بطولاتك تبثّ للمئات من داخل المنصة، والعميل يضغط زر واحد فقط.

---

## 🧭 نظرة سريعة على الرحلة

```
المرحلة ١: تجهيز أدوات ويندوز (١٠ دقائق)
المرحلة ٢: إنشاء حساب Oracle مجاني (٢٠ دقيقة)
المرحلة ٣: إنشاء الخادم (١٥ دقيقة)
المرحلة ٤: فتح المنافذ (١٠ دقائق)
المرحلة ٥: تثبيت برنامج البث (١٠ دقائق)
المرحلة ٦: ربط النطاق (١٠ دقائق)
المرحلة ٧: ربط المنصة (٥ دقائق)
✅ خلص — تجربة البث
```

نصيحة: خصّص وقتاً هادئاً، وافتح هذا الملف بجنب المتصفح.

---

## 🔧 المرحلة ١: تجهيز أدوات ويندوز

بتحتاج برنامجين مجانيين على ويندوز:

### أ) MobaXterm (للاتصال بالخادم)
هذا البرنامج يعطيك «الشاشة السوداء» (Terminal) للتحكم بالخادم، وهو الأسهل للمبتدئين على ويندوز.

1. افتح المتصفح وروح: `https://mobaxterm.mobatek.net/download-home-edition.html`
2. اضغط **Download** بجنب «Home Edition (Installer edition)».
3. بعد التحميل، افتح الملف المضغوط، شغّل ملف الـInstaller، واضغط Next حتى ينتهي.
4. افتح MobaXterm — بتشوف واجهة فيها زر أخضر كبير **Start local terminal**. لا تضغطه الآن، بس تأكّد إنه فتح.

خلّه مفتوحاً، بنرجع له في المرحلة ٣.

---

## ☁️ المرحلة ٢: إنشاء حساب Oracle Cloud المجاني

Oracle تعطي خادماً مجانياً **مدى الحياة** (مو تجربة تنتهي). بتحتاج بطاقة (للتحقق فقط — ما يخصمون شي على الخطة المجانية).

### الخطوات:
1. روح: `https://www.oracle.com/cloud/free/`
2. اضغط **Start for free**.
3. عبّي بياناتك:
   - **Country**: اختر بلدك (السعودية مثلاً).
   - البريد الإلكتروني والاسم.
4. تحقّق من بريدك (بيوصلك كود/رابط).
5. عبّي معلومات الحساب:
   - **Account type**: اختر **Individual** (فرد).
   - اسم الحساب (أي اسم، مثل اسم منصتك).
6. **البطاقة**: بيطلبون بطاقة للتحقق. بيسحبون مبلغ رمزي (~$1) ويرجعونه. **الخطة المجانية ما تُخصَم**.
7. اختر **Home Region**: مهم! اختر أقرب منطقة لك:
   - للسعودية/الخليج: **Saudi Arabia Central (Riyadh)** أو **UAE (Dubai)**.
   - ⚠️ هذا الاختيار **نهائي** — ما يتغيّر، فاختر الأقرب.
8. اضغط ووافق على الشروط، وانتظر إنشاء الحساب (ممكن ياخذ ٥-١٥ دقيقة، بيوصلك بريد).

بعد ما يجهز، سجّل دخول على: `https://cloud.oracle.com`

---

## 🖥️ المرحلة ٣: إنشاء الخادم (Instance)

الآن ننشئ الخادم المجاني نفسه.

1. بعد تسجيل الدخول، من القائمة (☰ فوق يسار) → **Compute** → **Instances**.
2. اضغط **Create Instance**.
3. **الاسم**: اكتب `batolat-stream` (أو أي اسم).
4. **Image and shape** (مهم جداً):
   - اضغط **Edit** بجنبها.
   - **Image**: اتركها Ubuntu لو ظهرت، أو اضغط Change Image واختر **Canonical Ubuntu 22.04**.
   - **Shape**: اضغط Change Shape → اختر تبويب **Ampere** → اختر **VM.Standard.A1.Flex**.
     - عدّل: **OCPUs = 2** و **Memory = 12 GB** (هذا ضمن المجاني).
   - اضغط Select Shape.
5. **Networking**: اتركه كما هو (بينشئ شبكة تلقائياً).
6. **Add SSH keys** (مهم — هذا مفتاح دخولك):
   - اختر **Generate a key pair for me**.
   - اضغط **Save private key** ← بيحفظ ملف باسم ينتهي بـ`.key` — **احفظه في مكان تتذكّره** (مثلاً مجلد باسم `oracle` على سطح المكتب). لا تفقده!
   - اضغط **Save public key** أيضاً احتياطاً.
7. اضغط **Create** تحت.
8. انتظر حتى تتحوّل حالة الخادم من برتقالي (Provisioning) إلى **أخضر (Running)** — دقيقتين تقريباً.
9. لمّا يصير أخضر، انسخ **Public IP address** (رقم مثل `140.238.x.x`) — بتحتاجه كثير. اكتبه بمكان.

---

## 🔓 المرحلة ٤: فتح المنافذ (خطوتان)

البثّ يحتاج «أبواب» مفتوحة في الخادم. لازم تفتحها من مكانين.

### أ) في شبكة Oracle (Security List):
1. من صفحة الخادم، انزل لقسم **Primary VNIC** → اضغط على اسم **Subnet** (رابط أزرق).
2. اضغط على **Default Security List** (رابط أزرق).
3. اضغط **Add Ingress Rules**، وأضف هذي القواعد **وحدة وحدة** (كرّر Add Ingress Rules لكل واحدة):

   | Source CIDR | IP Protocol | Destination Port |
   |---|---|---|
   | `0.0.0.0/0` | TCP | `8888` |
   | `0.0.0.0/0` | TCP | `8889` |
   | `0.0.0.0/0` | TCP | `1935` |
   | `0.0.0.0/0` | UDP | `8189` |
   | `0.0.0.0/0` | TCP | `443` |
   | `0.0.0.0/0` | TCP | `80` |

   (في كل مرة: Source Type = CIDR، Source CIDR = `0.0.0.0/0`، IP Protocol اختر TCP أو UDP، وв Destination Port Range اكتب الرقم، ثم Add Ingress Rules).

### ب) داخل الخادم (بيتم تلقائياً في المرحلة ٥ عبر السكربت — لا تشيل هم).

---

## 📥 المرحلة ٥: الاتصال بالخادم وتثبيت البث

الآن نتصل بالخادم عبر MobaXterm ونثبّت برنامج البث.

### أ) الاتصال:
1. افتح **MobaXterm**.
2. اضغط **Session** (فوق يسار) → **SSH**.
3. في **Remote host**: الصق الـPublic IP (رقم الخادم).
4. فعّل **Specify username** واكتب: `ubuntu`
5. اضغط تبويب **Advanced SSH settings** → فعّل **Use private key** → اختر ملف الـ`.key` اللي حفظته في المرحلة ٣.
6. اضغط **OK**. بيفتح شاشة سوداء وبيتصل. (أول مرة بيسأل عن الثقة، اضغط Accept/Yes).

الآن أنت داخل الخادم! 🎉

### ب) رفع ملفات البث:
1. في MobaXterm، على اليسار فيه لوحة ملفات (SFTP). لو ما ظهرت، اضغط تبويب **Sftp** على اليسار.
2. من مجلد `server-hls` (اللي عندك)، اسحب ملفّين لهذه اللوحة: `install.sh` و `mediamtx.yml`.

### ج) التثبيت (أمر واحد):
في الشاشة السوداء، اكتب هذا بالضبط واضغط Enter:
```
sudo bash install.sh
```
- بيشتغل لدقيقة-دقيقتين، وبينزّل ويثبّت كل شي تلقائياً.
- في النهاية بيطبع لك **عنوانين** — انسخهم واحفظهم:
  - عنوان النشر: `http://رقم-الخادم:8889/...`
  - عنوان المشاهدة: `http://رقم-الخادم:8888/...`

✅ الخادم الآن يشتغل! لكن باقي نربطه بنطاقك عشان يصير آمن (HTTPS) ويتحمّل أكثر.

---

## 🌐 المرحلة ٦: ربط النطاق (عبر Cloudflare — يعطيك HTTPS مجاني + قوة)

بما إن عندك نطاق، بنستخدم Cloudflare (مجاني) عشان:
- يعطي البث **HTTPS** (ضروري — بدونه المتصفح يرفض البث).
- يوزّع على مشاهدين أكثر.

### الخطوات:
1. سجّل مجاناً في `https://dash.cloudflare.com/sign-up`.
2. اضغط **Add a site** واكتب نطاقك (مثل `mysite.com`).
3. اختر الخطة **Free** واضغط Continue.
4. Cloudflare بيعرض لك **Nameservers** (سطرين مثل `xxx.ns.cloudflare.com`).
   - روح لمكان شريت منه النطاق (مزوّد النطاق)، وبدّل الـNameservers بتاعته بهذولا. (ابحث في مزوّدك عن «Nameservers» أو «DNS»).
   - بياخذ من ساعة لـ٢٤ ساعة حتى يتفعّل (عادة أسرع).
5. بعد ما يتفعّل، في Cloudflare روح **DNS** → **Add record**:
   - Type: **A**
   - Name: `stream`
   - IPv4 address: رقم خادم Oracle (Public IP)
   - Proxy status: **Proxied** (الغيمة برتقالية) ← مهم!
   - Save.

الآن صار عندك رابط بثّ آمن: `https://stream.نطاقك.com`

> ملاحظة: MediaMTX يخدم HLS على المنفذ 8888. مع Cloudflare Proxy، الوصول عبر `https://stream.نطاقك.com:8888`. (لو واجهت مشكلة منافذ مع Cloudflare، أخبرني وأعطيك إعداد Cloudflare Origin Rules لتوجيه المنفذ — خطوة بسيطة).

---

## 🔗 المرحلة ٧: ربط المنصة (٥ دقائق — الأسهل)

الآن نربط الخادم بمنصتك **مرة واحدة**، فتشتغل كل البطولات عليه تلقائياً.

1. افتح منصتك → صفحة **السوبر أدمن** (superadmin.html) وسجّل دخولك.
2. من القائمة الجانبية → **إعدادات المنصة**.
3. انزل لقسم **📡 خادم البث**.
4. في خانة **عنوان خادم النشر (WHIP)** اكتب:
   ```
   https://stream.نطاقك.com:8889
   ```
5. في خانة **عنوان المشاهدة عبر Cloudflare** اكتب:
   ```
   https://stream.نطاقك.com:8888
   ```
6. اضغط **💾 حفظ الإعدادات**.

✅ **خلص! كل بطولاتك الآن تبثّ للمئات من داخل المنصة تلقائياً.**

---

## 🎬 التجربة (تأكّد إن كل شي شغّال)

1. افتح أي بطولة → استوديو المذيع (broadcaster).
2. شغّل الكاميرا → اضغط **بدء البث**.
3. لو ظهرت رسالة «📡 بث احترافي عبر الخادم» → **مبروك، يشتغل!**
4. افتح صفحة الجمهور من جوال ثاني → افتح المباراة → لازم تشوف البث مع شريط النتيجة.

لو ظهرت رسالة «تعذّر البث للخادم — سيعمل P2P» → فيه خطأ في إعداد الخادم/المنافذ. راجع المرحلة ٤ (المنافذ) و٦ (Cloudflare)، أو أرسل لي الرسالة اللي تظهر وأساعدك بالضبط.

---

## 🆘 لو علقت في أي خطوة

اكتب لي: «علقت في المرحلة رقم X» واشرح وش صار / وش الرسالة اللي ظهرت، وأنا أعطيك الحل بالضبط لتلك الخطوة. لا تحاول تخمّن أو تكمّل وأنت غير متأكد — اسألني.

---

## 📌 ملخص العناوين اللي بتحتاج تحفظها

| العنصر | القيمة (عبّيها بنفسك) |
|---|---|
| رقم خادم Oracle (Public IP) | ________________ |
| ملف مفتاح الدخول (.key) | محفوظ في: ________ |
| عنوان النشر | https://stream.نطاقك.com:8889 |
| عنوان المشاهدة | https://stream.نطاقك.com:8888 |

احتفظ بهذا الملف — بترجع له لو احتجت.
