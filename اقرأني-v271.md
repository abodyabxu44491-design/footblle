# v271 — عودة نافذة المباراة إلى نسخة v217 العاملة

## ماذا في هذه النسخة

كل تطويرات v267 موجودة كما هي (المجموعات، الملحق، الذهاب/الإياب، البث،
بطاقات المشاركة، الترتيب، الإحصائيات …) **ما عدا مسار «إضافة مباراة»** —
فقد أُعيد حرفياً إلى كود v217 المؤكَّد عمله عندك.

## ما أُزيل من مسار الإضافة

بعد مقارنة النسختين، كل ما أُضيف حول `addMatch` في v267 أُزيل:

1. **منتقي الفرق الجديد** (أزرار داخل النافذة) → عاد `<select>` كما في v217.
2. **حوار «⚠️ فريق يلعب مرتين في نفس الجولة»** — كان يظهر مع الحوار القديم
   فيصير حوارَان متتاليان بنفس السؤال.
3. **قفل الزر** (`disabled` + «⏳ جارٍ الحفظ...») — كان يترك الزر ميتاً لو
   لم يعد الحوار بردّ.
4. **غلاف «حارس الحفظ المزدوج»** الذي يلفّ `addMatch` — هذا الغلاف **غير
   موجود إطلاقاً في v217**؛ أُضيف في v267 ويلفّ الدالة مرة ثانية فوق
   `lock-guard`. حُذف اسم `addMatch` من قائمته، وبقي يعمل على بقية الدوال.
5. **وضع «مباراة فاصلة بين مجموعتين»** — كان جزءاً من تلك الطبقات، فهو
   معطَّل مؤقتاً في هذه النسخة ويعطي رسالة واضحة عند الضغط عليه.

## ما أُبقي عليه من الإصلاحات

- **`viewport-lock.js`**: حارس منع الزوم ما عاد يستدعي `preventDefault` على
  `touchend` فوق العناصر التفاعلية — كان يُلغي حدث `click` نفسه، فتضيع أي
  ضغطة تقع خلال ٣٠٠ms من ضغطة قبلها.
- **مكافحة الكاش**: `sw.js` بنسخة `batolat-v271`، وكل مراجع JS/CSS في صفحة
  الإدارة بـ `?v=271`.
- زر الحفظ صار له `id="btnAddMatch"` — لا يغيّر السلوك، لكنه يسهّل أي تطوير
  قادم.

## الاختبارات

دوري عادي · نظام مجموعات (نفس المجموعة ومجموعتين مختلفتين) · فريق ضد نفسه ·
إضافات متتالية. في كل الحالات: المباراة تُضاف، النافذة تُغلق، ولا حوار عالق.

## الخطوة التالية

هذه نقطة انطلاق مستقرّة. أي تطوير للنافذة يُضاف **طبقة واحدة في كل مرة**
ويُجرَّب على الجوال قبل الطبقة التالية — فإن ظهر عطل نعرف مصدره فوراً بدل
البحث بين عدّة طبقات مضافة دفعة واحدة.

## بعد الرفع

امسح بيانات الموقع مرة واحدة (إعدادات المتصفح ← بيانات الموقع)، أو افتح
`F12 ← Application ← Service Workers ← Unregister` ثم `Ctrl+Shift+R`.
