# v333 — نواة مشتركة لبطاقة المباراة + ترتيب «الكل»

## ① ترتيب «الكل»: القادمة فوق المنتهية

```
1. المباشرة الآن
2. القادمة       (الأقرب موعداً أوّلاً)
3. المنتهية      (الأحدث أوّلاً)
```

القادمة تسبق المنتهية لأنها ما يعنيه المتابع الآن: **ما الذي سيُلعب؟**
والمنتهية مرجع يُرجع إليه بعدها.

## ② `match-core.js` — النواة المشتركة

المنصة فيها **ثلاث بطاقات مباراة مستقلّة** لكلٍّ احتياجاته: الإدارة فيها
أزرار تحرير وبثّ، والجمهور فيه توقّعات وشارات، والبطاقات فيها سجلّ ما
وُلّد. فدمجها في بطاقة واحدة يخنق الثلاثة.

**لكن الحقائق المشتركة بينها كانت مكرّرة:**

> ما اسم دور هذه المباراة؟ · كم طرداً على كل فريق؟ · ما ترتيبها؟

وكل نسخة تنحرف عن أختها مع أول تعديل. وهذا مصدر أخطاء متكرّرة في هذه
الجلسة: **«الجولة 1» على مباريات الإقصاء ظهرت في ثلاثة ملفات**، وعُولجت
في كلٍّ على حدة ثم عادت من ملف رابع.

الملف الجديد يحمل تلك الحقائق **مرة واحدة**. البطاقات تبقى ثلاثاً في
شكلها، وتقرأ من مصدر واحد في معناها.

### ما فيه

| الدالّة | ماذا تحسم |
|---|---|
| `stageLabel` | اسم الدور: إقصاء ← ملحق ← مجموعة ← جولة |
| `events` | الأحداث من `liveData` أثناء البثّ ومن `events` بعده |
| `redCount` | المطرودون لا أحداث الطرد — فيسقط التكرار |
| `status` | نصّ الحالة ولونها |
| `newestFirst` · `soonestFirst` · `allOrder` | قواعد الترتيب الثلاث |

### الملفات الستّة التي صارت تفوّض إليه

`viewer.js` · `cards-system.js` · `match-share-card.js` ·
`predictions.js` · `matches-tabs.js` · `admin-matches-tabs.js`

## الاختبارات

**سلسلة جديدة (`coretest`) بـ٣٠ فحصاً** كلها ✓:

- **اسم الدور في سبع حالات** — ومنها «إقصاء بالعَلَم فقط» و«إقصاء بالمعرّف
  فقط»، وهما اللتان كانتا تسقطان إلى «الجولة N».
- **عدّ الطرد في خمس حالات** — ومنها «صفراوان + حمراء لنفسه» و«حمراء
  مرتين لنفسه»، وكلتاهما تُعدّ طرداً واحداً.
- **الترتيب**: `live → up1 → up2 → fin1 → fin2` ✓، وإقصاء بلا تواريخ
  يبدأ بالنهائي ✓.
- **الملفات الستّة تفوّض فعلاً**، والنواة محمَّلة في الصفحتين وفي `sw.js`.

وأُعيد تشغيل **تسع سلاسل** سابقة بعد تحديثها لتختبر النواة — صفر أخطاء.

## المعاينة

`معاينة-قوائم-المباريات.html` — الأقسام الثلاثة بالترتيب الجديد.

## بعد الرفع

`batolat-v333` وكل المراجع `?v=333`. **ملف جديد: `match-core.js`** —
تأكّد من رفعه. ثم امسح بيانات الموقع مرة واحدة.
