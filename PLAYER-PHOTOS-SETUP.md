# 📷 صور اللاعبين — دليل التفعيل والتشغيل (v219)

> ⚠️ **تصحيح مهم:** النسخة السابقة من هذا الدليل كانت تقول إن الصور تُرفع إلى
> **Firebase Storage**. هذا **غير صحيح** — الكود يرفع إلى **Cloudinary**
> (`admin.js` سطر 43-44). ملف `storage.rules` لا علاقة له بصور اللاعبين إطلاقاً،
> وتفعيله لم يكن ليحلّ أي مشكلة رفع. الدليل الآن مطابق للواقع.

الصور تُخزَّن في **Cloudinary**، ويُحفظ في Firestore **رابط نصّي قصير فقط** — فلا
تستهلك صور اللاعبين شيئاً يُذكر من حصة قاعدة البيانات.

---

## ⚙️ خطوة إعداد واحدة مطلوبة (مرّة واحدة)

**بدونها:** الرفع والتغيير يعملان بشكل كامل، لكن **الحذف الفوري** من Cloudinary
لن يعمل — وستُسجَّل الملفات في «سلّة الصور» لتنظّفها يدوياً لاحقاً.

1. افتح **Cloudinary Console** → **Settings** (الترس) → **Upload**.
2. انزل إلى **Upload presets** → افتح الـ preset المستخدم: **`wvebrqwq`**.
3. تأكّد أن **Signing Mode = Unsigned**.
4. فعّل الخيار **«Return delete token»** ثم **Save**.

هذا الخيار يجعل Cloudinary يُرجع مع كل صورة مرفوعة «توكن حذف» صالحاً ~10 دقائق،
وهو الطريقة الوحيدة لحذف ملف من المتصفح بلا وضع مفتاح سرّي في كود عام.

---

## 🐞 الأعطال التي عُولجت في هذه النسخة

### ① «أغيّر الصورة فلا تتغيّر»

**السبب الجذري:** كان معرّف الصورة ثابتاً (`player_<الفريق>_<اللاعب>`)، ورفع
Cloudinary غير الموقّع **لا يسمح بالاستبدال إطلاقاً** — الخيار `overwrite` ممنوع
في الرفع غير الموقّع ويبقى `false` دائماً. فعند رفع صورة جديدة بنفس المعرّف كان
Cloudinary **يُرجع الأصل القديم كما هو بنفس الرابط**، فيُحفظ نفس الرابط في قاعدة
البيانات ولا يتغيّر شيء عند الجمهور. لم تكن مشكلة كاش متصفح — الملف نفسه لم يُستبدل.

**الحل:** معرّف فريد لكل رفعة (`…_<طابع زمني>`) → أصل جديد ورابط جديد في كل مرة،
فالتغيير مضمون 100% بلا اعتماد على أي كاش.

### ② «أحذف الصورة فلا تُحذف ولا تُفرَّغ المساحة»

**سببان منفصلان:**
1. **زر الحذف لم يكن موجوداً في الواجهة أصلاً.** الدالة `removeRosterPhoto` كانت
   معرّفة في الكود، لكن **لا يستدعيها أي زر** — فحذف الصورة كان مستحيلاً من لوحة
   التحكم مهما حاولت.
2. وحتى لو استُدعيت، كانت تمسح الرابط من Firestore فقط — **والملف يبقى في
   Cloudinary للأبد** يستهلك الحصة.

**الحل:**
- أُضيف زر **🚫** بجانب كل لاعب له صورة (يظهر فقط لمن لديه صورة).
- الحذف صار حقيقياً عبر `delete_by_token` — الملف يُمحى من Cloudinary وتُفرَّغ مساحته.
- ما يتعذّر حذفه فوراً (انتهت صلاحية التوكن) يُسجَّل في **سلّة الصور** بمعرّفه،
  فلا يضيع أي ملف مجهولاً داخل الحصة.
- **عند استبدال صورة**: القديمة تُحذف تلقائياً — لا تتراكم النسخ.
- **عند حذف اللاعب نفسه**: صورته تُحذف معه — لا تبقى ملفات يتيمة.

### ③ «الصور تختلط بين اللاعبين المتشابهين في الاسم»

**السبب الجذري (أخطرها):** دالة جلب الصورة كانت — عند عدم إيجاد صورة داخل فريق
اللاعب — **تمسح كل فرق البطولة** وترجع صورة أي لاعب يحمل نفس الاسم:

```js
for (const tid in window._teamRosters) { ... }   // ← التسريب
```

فـ«محمد» في فريق أ كان يظهر بصورة «محمد» في فريق ب. وداخل الفريق الواحد، المطابقة
بالاسم وحده كانت تعطي صورة الزميل المتشابه في الاسم.

**القواعد الجديدة** (أول قاعدة تحسم تُنهي البحث):

| # | القاعدة | السلوك |
|---|---|---|
| ① | **الهوية (id)** | مرجع مطلق. وُجد السجل → صورته أو لا شيء. لا انتقال للاسم بعدها. |
| ② | **الاسم + رقم القميص** | يفصل بدقة بين المتشابهين داخل الفريق. |
| ③ | **الاسم وحده** | **فقط** إن كان فريداً في الكشف. تكرّر بلا رقم → فراغ. |
| ④ | **عبور الفرق** | ممنوع منعاً باتّاً. بلا `teamId` → لا صورة. |

**المبدأ:** عرض ظلّ اللاعب الافتراضي أصدق من عرض صورة شخص آخر.

وحُذفت كل نسخ المطابقة بالاسم المتفرّقة (كانت في 3 مواضع مختلفة) ووُحّدت في مصدر
واحد `_lineupPhoto`، فلا يعود الخلل من باب خلفي عند تعديل نسخة دون أخرى.

---

## ✅ نتائج الاختبار الفعلي

**18 اختبار وحدة — نجحت كلها (0 فشل):**

سيناريوهات تشابه الأسماء (14): تسريب بين الفرق · «محمد» في فريقين · «محمد» مكرّر
داخل الفريق الواحد بأرقام مختلفة · الهوية بلا صورة لا تستعير من الشبيه · هوية
خاطئة · بلا فريق · تطبيع التشكيل والمسافات العربية.

دالة صور الإحصائيات (4): تشترك في نفس المصدر والقواعد.

**11 اختبار للرفع/الحذف ضد محاكاة لسلوك Cloudinary الحقيقي — نجحت كلها:**
أُثبت فيها العطل القديم عملياً (نفس المعرّف = نفس الرابط)، وأن المعرّف الفريد
يُنتج رابطاً جديداً في كل رفعة، وأن `delete_by_token` يمحو الأصل فعلياً، وأن
التوكن المنتهي يفشل بأمان فيُسجَّل في السلّة.

---

## 📖 طريقة الاستخدام

| العملية | الخطوات |
|---|---|
| **إضافة صورة** | الفرق → كشف الفريق → زر **📷** بجانب اللاعب |
| **تغيير صورة** | نفس زر **📷** — القديمة تُحذف تلقائياً |
| **حذف صورة** | زر **🚫** (يظهر لمن له صورة فقط) |
| **سلّة الصور** | زر **🧹** أعلى نافذة الكشف |

الصورة تُضغط تلقائياً إلى ~45KB بمقاس 256×256 (WebP ثم JPEG احتياطاً)، وتُقصّ
مربّعة من المنتصف لأفضل تأطير للوجه.

**أين تظهر للجمهور:** التشكيلة على الملعب · مقاعد البدلاء · نافذة إحصائيات اللاعب ·
جداول الهدّافين والصنّاع · بطاقات المشاركة. ومن بلا صورة يظهر بظلّ لاعب أنيق.

---

## 🗃️ الحقول الجديدة على مستند اللاعب

```
photo            رابط الصورة (كما كان)
photoPublicId    معرّف الأصل في Cloudinary — لازم للحذف
photoDeleteToken توكن الحذف (صالح ~10 دقائق)
photoAt          وقت الرفع
```

وسلّة المهملات في `leagues/{id}/photoTrash` — مغطّاة بقاعدة `match /{sub}/{docId}`
الموجودة في `firestore.rules`، فلا تحتاج أي تعديل على القواعد.
