# نافذة المباراة — إعادة بناء كاملة (MM2)

النافذة القديمة حُذفت بالكامل: تصميمها، ومنطق اختيار الفرق، ومسار الحفظ.
هذا وصف ما حلّ محلّها ولماذا.

## لماذا أُعيد البناء

التصميم القديم كان يعتمد على **حوارات تأكيد فوق النافذة** (`confirmDialog`)
تعترض الحفظ، وعلى **قفل الزر** حتى يعود ردّ الحوار. النتيجة أن أي حوار لا
يراه المنظّم — أو ضغطة لمس تضيع قبل أن تصل — تترك الزر مقفولاً بلا رسالة،
فيبدو ميتاً تماماً. المشكلة كانت في البنية نفسها، لا في سطر يُصلَّح.

## المبدأ الجديد: لا حوار فوق النافذة إطلاقاً

- **شريط حالة حيّ** داخل النافذة يتحدّث مع كل اختيار — يعرف المنظّم الحالة
  *قبل* أن يضغط، لا بعدها.
- **أحمر = مانع**: لا تُحفظ. ضغط الزر يهزّ الشريط ويمرّره للعرض ويشرح السبب.
- **ذهبي = تحذير**: تُحفظ بضغطة واحدة، ونصّ الزر نفسه يتحوّل إلى
  «⚠️ أضِف رغم التحذير» فلا مفاجأة.
- **الزر لا يُعطَّل أبداً** (`disabled`) — كل ضغطة تُنتج ردّ فعل مرئي.

## التصميم

- **بطاقة مواجهة** بالأعلى: المضيف × الضيف بشعاريهما واسم مجموعة كلٍّ منهما،
  وزرّ دائري بينهما لتبديل الأرضية. الضغط على أي جهة يزيل اختيارها.
- **بحث فوري** بالاسم فوق قائمة الفرق.
- **قائمة الفرق كأزرار** مقسّمة برؤوس المجموعات، مع شارة «مضيف»/«ضيف» على
  المختار. الفرق بلا شعار تحصل على حرف أوّل ملوّن بدل فراغ.
- **الحقول الأساسية** (تاريخ · وقت · ملعب · جولة · دور المواجهة) في شبكة
  عمودين، و**التفاصيل الاختيارية** (حكّام · رعاة · ملاحظات) مطوية خلف زرّ
  واحد — فالنافذة قصيرة ولا تحتاج تمريراً طويلاً.
- **تذييل ثابت** خارج منطقة التمرير: زر الحفظ لا ينزاح تحت الإصبع لحظة الضغط.
- كل الأهداف اللمسية 44px فأكثر.

## طبقة الأحداث

لا يوجد `onclick` واحد في النافذة. كل الأزرار تحمل `data-mm2-*`، وتُلتقط
بالتفويض من عنصر النافذة نفسه — فإعادة رسم القائمة لا تفقد أي ربط.

`click` هو المسار الأساسي. ولأن الجوال قد يبتلعه (حارس زوم يستدعي
`preventDefault` على `touchend`، أو انزياح تخطيط لحظة اللمس)، نستمع لـ
`pointerup` كمسار احتياطي **مؤجّل 380ms** يُلغى فور وصول `click`. النتيجة:
تنفيذ واحد دائماً، ولا تُبتلع ضغطتان متتاليتان سريعتان.

## الحفظ

بلا قفل دائم. أثناء الكتابة يظهر «⏳ جارٍ الحفظ...»، ومعه حارس ٢٠ ثانية
يعيد الزر لحالته مع رسالة عن الاتصال إن تأخّرت الشبكة. الضغط أثناء الحفظ
يعطي رسالة بدل الصمت. بعد النجاح تبقى النافذة مفتوحة ويُصفَّر الفريقان —
فإضافة جولة كاملة تصير سلسلة ضغطات بلا إعادة فتح.

## التوافق

الحقلان المخفيان `matchHome` و`matchAway` يبقيان محدَّثين دائماً، وكل
معرّفات الحقول الأخرى (`matchDate`, `matchTime`, `matchVenue`, `matchRound`,
`matchLeg`, `matchReferee` … ) كما هي — فأي كود آخر في المنصة يقرأها يعمل
بلا تغيير. ووضع «المباراة الفاصلة بين مجموعتين» مدعوم كما كان.

## ما حُذف من الكود

`_teamSections` · `_tpLogo` · `_tpPick` · `_tpSwap` · `_tpRenderPicked` ·
`_tpSameGroupOnly` · `_renderTeamPicker` · `populateMatchSelects` القديمة ·
`_prefillMatchModal` · `_addMatchInner` · فرع `modal-match` داخل `openModal`.
بقيت أسماء عامة قليلة كأسماء مستعارة (aliases) للنسخة الجديدة حتى لا ينكسر
أي نداء قديم في ملف آخر.

## الاختبارات المُجراة

دوري عادي · نظام مجموعات · فريق واحد فقط ثم ضغط الحفظ · فريقان من مجموعتين
مختلفتين · مواجهة مكررة · البحث · تبديل الأرضية · إزالة اختيار · طيّ وفتح
التفاصيل · الإغلاق · **محاكاة ابتلاع `click`** (يعمل عبر المسار الاحتياطي) ·
**ثلاث ضغطات سريعة على الحفظ** (تُضاف مباراة واحدة فقط).
