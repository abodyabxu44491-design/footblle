# v193 — تقوية البثّ المجاني P2P (أقوى ما يمكن ضمن حدوده)

بعد اختيار P2P المجاني كحلّ أساسي، قُوّي بأربعة تطويرات ترفع موثوقيته كثيراً —
كلها مجانية بلا أي تعقيد على المستخدم.

## ① TURN جاهز (يرفع نسبة الاتصال الناجح لـ ~99%)

المشكلة: STUN وحده يفشل مع ~8-10% من المشاهدين (شبكات الجوال/CGNAT/جدران صارمة)
— لا يتصلون إطلاقاً. الحل: أُضيف مكان جاهز لـ TURN المجاني (Open Relay من metered.ca،
20GB/شهر مجاناً). صاحب البطولة يضع مفتاحه المجاني في `TURN_CONFIG` (المذيع) و
`window._psTurn` (الجمهور) فيقفز الاتصال الناجح لـ ~99%. بدونه يعمل بـ STUN عادي.
أُضيفت أيضاً STUN من Cloudflare كخيار رابع.

## ② إعادة اتصال تلقائي للمشاهد

لو انقطع اتصال المشاهد (شبكة متذبذبة)، يُعيد المحاولة تلقائياً حتى 5 مرات بتباعد
متزايد (1→4 ثوان) بدل شاشة سوداء. عند النجاح يُصفّر العدّاد. رسائل واضحة:
«جارِ الاتصال» / «تُعاد المحاولة» / «تأكد أن البث ما زال مباشراً».

## ③ جودة تكيّفية حسب عدد المشاهدين

سقف معدل البت يتغيّر تلقائياً مع العدد ليوزّع رفع المذيع بذكاء ويتحمّل أكثر بثبات:

| المشاهدون | الجودة لكل مشاهد |
|---|---|
| ≤ 5 | 900 kbps (عالية) |
| 6-12 | 650 kbps |
| 13-25 | 450 kbps |
| 25+ | 300 kbps (موفّر) |

يُعاد التوزيع على الجميع تلقائياً كلما انضمّ/غادر مشاهد.

## ④ مؤشرات حالة أوضح

عدّاد المشاهدين يتلوّن (أصفر 15+، أحمر 25+) وحالة الاتصال «متصل/غير متصل» فورية.

## الاختبار (نجح)
- منطق الجودة التكيّفية: 8/8 (900→650→450→300 حسب العدد). ✅
- منطق إعادة المحاولة: 5 محاولات بتباعد متزايد ثم توقف (لا حلقة لانهائية). ✅
- فحص صياغة `node --check`: `viewer.js` · `broadcaster.html`. ✅

## الحدود (بصراحة)
P2P يبقى مناسباً لعشرات المشاهدين (15-50 حسب شبكة المذيع). هذه التطويرات ترفع
**الموثوقية والثبات** ضمن هذا المدى، لا تكسر حدّه الفيزيائي. لأعداد أكبر: يوتيوب
مضمّن (مجاني) أو HLS احترافي (على حساب صاحب البطولة) — كلاهما جاهز في المنصة.

## النشر
ارفع: `viewer.js` · `broadcaster.html` · `sw.js` (v93).
(اختياري) لتفعيل TURN: ضع مفتاح metered.ca المجاني في الملفين.

---
---

# v192 — البثّ الاحترافي (HLS · آلاف المشاهدين · صفر تكلفة على المنصة)

## الفكرة الصحيحة (بعد إعادة التفكير)

أنت تبيع **النظام كاملاً** لكل صاحب بطولة. إذن التكلفة يجب أن تكون **عليه لا عليك**.
الحل الأمثل: لا تبني بنية توزيع فيديو، بل اجعل النظام **يحتضن أي مصدر بثّ احترافي**
يجهّزه صاحب البطولة بنفسه — والتوزيع على حسابه.

```
صاحب البطولة ──يبثّ من OBS/كاميرا──▶ مزوّده (Cloudflare Stream/أي CDN بحسابه هو)
                                              │ يوزّع HLS (يتحمّل آلاف)
                                              ▼
                                     رابط m3u8 يلصقه في الإدارة
                                              │
                              مشغّل hls.js في صفحة الجمهور
                                     + شريط النتيجة/الوقت الحيّ فوقه
```

**النتيجة:**
- صفر تكلفة على المنصة (التوزيع على حساب صاحب البطولة). ✅
- آلاف المشاهدين للبثّ الواحد (HLS احترافي، ليس P2P). ✅
- جودة عالية (بثّ OBS/كاميرا احترافية). ✅
- تبيع النظام كاملاً وكل بطولة مستقلة بمصدرها. ✅
- تميّزك الأسطوري باقٍ: شريط النتيجة/الوقت الحيّ فوق **أي** بثّ.

## ما بُني

**مشغّل HLS احترافي (`viewer.js`):**
- عند لصق رابط `.m3u8` في الإدارة → يشتغل مشغّل **hls.js** تلقائياً.
- يعمل على **كل المتصفحات**: كروم/فايرفوكس/أندرويد عبر hls.js، وSafari/iOS أصلياً.
- وضع زمن-منخفض (low-latency) قريب من الحيّ، وإصلاح تلقائي عند تعثّر الشبكة.
- المكتبة تُحمّل عند الحاجة فقط (لا تُبطئ الصفحة).
- تنظيف كامل عند إغلاق التفاصيل (لا تسرّب).

**شريط النتيجة/الوقت فوق البثّ الاحترافي:**
نفس الشريط (من liveData عبر TimerCore) يظهر فوق مشغّل HLS تلقائياً — فتجربة
المشاهد أغنى من يوتيوب: فيديو احترافي + نتيجة ووقت حيّ متزامنان مع الإدارة.

**واجهة الإدارة (`admin.js`):**
حقل الرابط يوضّح دعم m3u8، وزر بثّ المنصة P2P (v188) يبقى كخيار مجاني للصغار.

## الطبقات الثلاث المتاحة الآن لصاحب البطولة

| الطبقة | التحمّل | التكلفة عليه | الأنسب لـ |
|---|---|---|---|
| **HLS احترافي (m3u8)** | آلاف | رمزية (حسابه) | البطولات الكبيرة |
| يوتيوب/تويتش (رابط) | آلاف | صفر | من عنده قناة |
| P2P من الجوال (v188) | عشرات | صفر | البطولات الصغيرة |

**كلها** تعرض شريط النتيجة/الوقت الحيّ فوقها.

## الاختبار (نجح)

- **منطق مشغّل HLS:** متصفح عادي → hls.js (loadSource+attach+play). Safari →
  تشغيل أصلي. كلاهما ✅.
- **تصنيف الروابط:** m3u8 (حتى مع token) → HLS، يوتيوب/mp4 كما هي. ✅
- فحص صياغة `node --check`: `viewer.js` · `admin.js` نجح.

## النشر
ارفع: `viewer.js` · `admin.js` · `sw.js` (v92).
صاحب البطولة يلصق رابط m3u8 في صفحة البث بالإدارة → يظهر احترافياً للجمهور.

---
---

# v191 — الانضمام المتأخر مضمون + جودة موفّرة (أعلى تحمّل)

## ① الانضمام المتأخر — الوقت والنتيجة يظهران فوراً

لو المذيع (أو أي مشاهد) فتح البث **متأخراً**، يظهر فوراً الوقت الذي وصلت له
الإدارة والنتيجة الحالية — **لا يبدأ من الصفر**. السبب: الوقت يُحسب من لحظة بدء
الإدارة المخزّنة (`half1StartedAt`) عبر `TimerCore`، لا من لحظة فتح البث. و
`onSnapshot` يعطي القيمة الحالية فور الاشتراك.

**مُثبَت بالاختبار:**
- المذيع فتح بعد 37:12 من بداية الإدارة → ظهر `37:12` فوراً (نتيجة 2-1 معه). ✅
- الشوط الثاني (45+8) → ظهر `53:00` صحيحاً. ✅
- مباراة لم تبدأ → لا وقت (0:0 بلا عدّاد). ✅

الشريط الآن أكثر صلابة: يستمر ما دامت حاوية البث ظاهرة، ولا يتعطّل لو تأخّرت
قائمة المباريات لحظة.

## ② كم يتحمّل؟ (إجابة صريحة)

القيد = **رفع جوال المذيع** ÷ حجم البث لكل مشاهد (P2P يرسل نسخة لكل مشاهد).
لذا خُفضت الجودة الافتراضية إلى **480p/24fps** مع **سقف 700kbps لكل مشاهد** —
واضحة تماماً لمباراة، وتضاعف العدد الممكن:

| الجودة | شبكة ضعيفة | متوسطة | جيدة 4G | 5G/ألياف |
|---|---|---|---|---|
| 720p (قديم) | 1 | 4 | 9 | 24 |
| **480p (الجديد)** | **3** | **9** | **18** | **50** |
| 360p (تلقائي عند الضعف) | 6 | 18 | 37 | 100 |

المتصفح **يخفض الجودة تلقائياً** لو ضعفت الشبكة (تكيّف تلقائي) — فالبث يثبت بدل
أن ينقطع. ومؤشّر المشاهدين عند المذيع يتحوّل لأصفر (15+) ثم أحمر (25+) كتنبيه.

**الخلاصة الصادقة:** P2P مضمون ومستقر حتى **~15-50 مشاهد** حسب شبكة المذيع
(الأغلبية الساحقة من بطولات الأحياء داخل هذا المدى). للوصول الثابت إلى 200 يلزم
مستقبلاً طبقة توزيع خفيفة (SFU) — وبنية الإشارة الحالية جاهزة لها دون إعادة كتابة.

## التغييرات
- `broadcaster.html`: جودة 480p موفّرة + سقف bitrate + تنبيه سعة + كتم ضوضاء الصوت.
- `viewer.js`: شريط النتيجة أكثر صلابة عند الانضمام المتأخر.
- `sw.js`: v91.

---
---

# v190 — فصل الأدوار: الإدارة تدير، التطبيق يبثّ فيديو فقط

إعادة تصميم البثّ وفق تصوّر واضح: **شخصان، دوران منفصلان**.

## النموذج

| الدور | من | يفعل |
|---|---|---|
| **مدير المباراة** | شخص في لوحة الإدارة | يدير المباراة بثّاً عادياً: يبدأ الوقت، يسجّل الأهداف والبطاقات، النتيجة — **بدون فيديو** |
| **المذيع** | شخص في تطبيق البث | يبثّ **الفيديو فقط**. لا أزرار أحداث ولا نتيجة — كلها تأتيه تلقائياً |

## كيف تتدفّق البيانات

```
مدير المباراة (admin) ──يبدأ الوقت/يسجّل هدف──▶ m.liveData في Firestore
                                                      │
                          ┌───────────────────────────┼───────────────────────────┐
                          ▼                           ▼                           ▼
                  شريط استوديو المذيع        شريط النتيجة فوق البث           لوحة النتيجة العادية
                  (broadcaster.html)          (league-viewer)                  (كما هي)
                          └────────── الثلاثة يقرؤون نفس liveData عبر TimerCore ──────────┘
```

- **الوقت:** يُحسب بـ `TimerCore.compute` نفسه في الأماكن الثلاثة → مطابق **للثانية**.
- **الوقت لا يعمل** حتى يبدأ مديرُ المباراة الشوطَ من الإدارة (`matchStatus='live'`).
  قبل ذلك يظهر البثّ بنتيجة 0:0 بلا عدّاد.
- **النتيجة** تتحدّث تلقائياً لحظة ما يسجّل المدير هدفاً (`homeScore/awayScore`).

## التغييرات

| الملف | التغيير |
|---|---|
| `broadcaster.html` | صار للبثّ فقط: أُزيلت أزرار التعليق/الأحداث. ضُمّن `TimerCore` (منسوخ من league-viewer) فصار الوقت مطابقاً 100%. جلب إعدادات البطولة للمدد |
| `viewer.js` | شريط النتيجة/الوقت فوق البث يقرأ `liveData` عبر TimerCore. أُزيل التعليق (الأحداث تأتي من نظام أحداث المباراة الذي يديره المدير) |
| `admin.js` | زر «بثّ مباشر من المنصة» يفتح التطبيق للمباراة (بدء تلقائي) |
| `sw.js` | v90 |

## الاختبار (نجح)

- **تزامن TimerCore:** حُمّل TimerCore في بيئة مستقلة وحُسب الوقت من `liveData`
  بدأتها الإدارة قبل 3:20 → الناتج `03:20` بالضبط. ومباراة لم تبدأ → لا وقت.
  يثبت أن وقت المذيع = وقت الجمهور = وقت الإدارة.
- فحص صياغة نجح: `viewer.js` · `admin.js` · `broadcaster.html` (module + TimerCore).
- (مصافحة WebRTC والتكامل من v188 سارية.)

## النشر
ارفع: `broadcaster.html` · `viewer.js` · `admin.js` · `sw.js`
انشر القواعد (v188): `firebase deploy --only firestore:rules`

---
*(v188 = المعمارية والأمان · v189 = البدء التلقائي والشريط · v190 = فصل الأدوار.)*

---
---



تطوير نظام البثّ (v188) ليكون **أسطورياً بلا تعقيد**:

## ما الجديد

### ① بدء تلقائي بضغطة من الإدارة
في لوحة الإدارة، صفحة البث، ظهر زر بارز **«بثّ مباشر من المنصة — بدون يوتيوب»**.
بالضغط يفتح استوديو المذيع للمباراة، ويبدأ **الكاميرا ثم البث تلقائياً** — بلا إدخال يدوي.
(الرابط: `broadcaster.html?league=X&match=Y&auto=1`.)

### ② شريط نتيجة ووقت حيّ فوق البث
يظهر شريط زجاجي أنيق أعلى الفيديو: شعار واسم الفريقين، النتيجة، والوقت الجاري —
بالضبط مثل بث المباريات التلفزيونية. **يتحكّم فيه المنظّم من لوحة الإدارة**، لأنه يقرأ
من `m.liveData` نفسه الذي تحدّثه أزرار البث الحالية (جول/وقت/شوط). يتحدّث لحظياً
وكل ثانية.

### ③ ظهور تلقائي مكان الفيديو
حين يبدأ البث، يظهر تلقائياً في صفحة الجمهور **مكان مشغّل الفيديو** بأولوية على أي
رابط خارجي — دون أي إجراء من المشاهد.

### ④ شريط النتيجة داخل استوديو المذيع أيضاً
المذيع يرى نفس النتيجة والوقت الحيّ في استوديوه (مستمع حيّ على المباراة) — فيطمئن
أنّ ما يبثّه متزامن مع بيانات الإدارة.

## الملفات المعدّلة

| الملف | التغيير |
|---|---|
| `admin.js` | زر «بثّ مباشر من المنصة» + `lpOpenBroadcaster()` |
| `broadcaster.html` | بدء تلقائي (`auto=1`) + شريط نتيجة/وقت حيّ في الاستوديو |
| `viewer.js` | شريط نتيجة/وقت فوق البث (`_psUpdateScorebar`) + مؤقّت ثانية |
| `sw.js` | نسخة الكاش → v89 |

## مبدأ التحكم (كما طلبت)

النتيجة والوقت **مصدرهما الوحيد** `m.liveData` الذي يحدّثه المنظّم من أزرار البث في
الإدارة. المذيع لا يكرّر إدخالها — يبثّ الفيديو فقط، والبيانات تتدفّق تلقائياً من الإدارة
إلى: شريط الجمهور فوق البث، وشريط استوديو المذيع، ولوحة النتيجة العادية. مصدر واحد،
ثلاثة عروض متزامنة.

## الاختبار

- تصيير بصري فعلي (Chromium) لشريط النتيجة فوق البث → يظهر احترافياً: شعار +
  اسم + نتيجة + وقت أحمر، بخلفية زجاجية.
- فحص صياغة `node --check` نجح لـ `viewer.js` · `admin.js` · `broadcaster.html`.
- (اختبارات v188 لمصافحة WebRTC والتكامل ما زالت سارية.)

## النشر
ارفع: `admin.js` · `broadcaster.html` · `viewer.js` · `sw.js`
(القواعد `firestore.rules` من v188 كما هي — تأكد أنها منشورة.)

---
*(انظر BROADCAST-SYSTEM.md — v188 — للمعمارية الأساسية وقواعد الأمان.)*

---

## الفكرة المعمارية

```
تطبيق المذيع (broadcaster.html — مستقل)
   │  يفتح الكاميرا (WebRTC) + لوحة تعليق حي
   │
   ├─ الفيديو ─────────────────┐  (P2P مباشر، لا يمرّ بأي خادم = صفر تكلفة)
   │                           ▼
   │                   أجهزة المشاهدين (league-viewer)
   │
   └─ الإشارة فقط (offer/answer/ICE نصوص صغيرة) عبر Firestore ← مجاني
```

**لماذا التكلفة صفر؟** خوادم الفيديو التقليدية تدفع مقابل كل ميغابايت يشاهده الجمهور.
هنا الفيديو ينتقل مباشرة بين الأجهزة (Peer-to-Peer). Firestore يُستخدم فقط لتبادل
"المصافحة" الأولية (بضع كيلوبايت نصية)، وخوادم STUN من Google مجانية ولا يمرّ عليها
فيديو.

---

## الملفات

| الملف | التغيير |
|---|---|
| `broadcaster.html` | **جديد** — تطبيق المذيع المستقل الكامل |
| `viewer.js` | مستقبِل البث (WebRTC) + التعليق الحي + تصدير `_fsDb` |
| `firestore.rules` | قواعد آمنة لـ `liveStreams` |
| `sw.js` | نسخة الكاش → v88 |

---

## تطبيق المذيع (broadcaster.html)

**شاشة الإعداد:** معرّف البطولة → اختيار المباراة (يُحمّل تلقائياً) → اسم المذيع →
**تسجيل دخول بحساب المنظّم** (مطلوب أمنياً لبدء البث).

**الاستوديو:**
- معاينة كاميرا مباشرة (أمامية/خلفية، قلب، كتم صوت).
- زر **بدء/إيقاف البث** كبير.
- عدّاد مدة + عدّاد مشاهدين حيّ + رابط مشاركة.
- **لوحة تعليق حيّ:** أزرار سريعة (هدف/أصفر/أحمر/تبديل) + حقل نص حر → يظهر
  فوراً للجمهور (نصوص فقط = مجاني، يتحمّل أي عدد).

## جانب الجمهور (viewer.js)

عند فتح تفاصيل مباراة **مباشرة**:
1. يشترك في `liveStreams/{league__match}`.
2. لو البث نشط → يُنشئ اتصال WebRTC ويعرض الفيديو مع شارة "مباشر".
3. يعرض التعليق الحي أسفل الفيديو، ويحدّثه لحظياً.
4. عند إغلاق التفاصيل → يُغلق الاتصال ويُحرّر مقعده (تنظيف تام).

---

## الأمان (firestore.rules)

```
liveStreams/{sid}
  read:  عام (المشاهدة متاحة للجميع)
  write: منظّم البطولة فقط (المذيع مسجّل دخول) — sid يحمل leagueId للتحقق
  viewers/{vid}:
    read/write: عام — لكنه offer/ICE فقط، لا يمسّ بيانات البطولة
```

- المذيع **لا يكتب على مستند المباراة إطلاقاً** — كل شيء في `liveStreams` المعزول.
- المشاهد المجهول يكتب مقعده الخاص فقط (id عشوائي)، فلا يعبث ببثّ غيره.
- قاعدة `{document=**}` النهائية تبقى تمنع كل ما عداه.

---

## الاختبارات (نجحت 100%)

1. **مصافحة WebRTC حقيقية** (Chromium + fake media): المذيع↔المشاهد →
   الفيديو وصل فعلياً + الحالة `connected`.
2. **تكامل كامل بمنطق المنصة الفعلي** عبر Firestore محاكى: نفس دوال المذيع
   والمشاهد → البث وصل من الطرف للطرف.
3. فحص صياغة `node --check` نجح لـ `viewer.js` و `broadcaster.html`.

---

## حدود النظام (المصارحة)

WebRTC P2P ممتاز حتى **عشرات المشاهدين لكل مذيع**. مع اقتراب ٢٠٠ مشاهد، رفع جوال
المذيع (upload) قد لا يكفي لإرسال نسخة منفصلة لكلٍّ منهم. الحلول المستقبلية عند الحاجة:
- **الطبقة المجانية دائماً كافية:** التعليق الحي + النتيجة + الملعب المتحرك تعمل لأي
  عدد بلا حدود (نصوص فقط).
- عند الحاجة لفيديو لمئات: تُضاف طبقة SFU (خادم توزيع خفيف) لاحقاً — بنية الإشارة
  الحالية جاهزة لها دون إعادة كتابة.

---

## النشر

1. ارفع: `broadcaster.html` · `viewer.js` · `sw.js`
2. انشر القواعد: `firebase deploy --only firestore:rules`
3. رابط المذيع: `.../broadcaster.html?league=SLUG` (أو يُدخلها يدوياً).

**تنبيه:** بدون نشر `firestore.rules` سيُرفض البث (القواعد الحالية تمنع `liveStreams`).
