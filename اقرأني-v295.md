# v295 — «هدف بلا اسم» في الواجهات الثلاث + حارس ملفات الشيفرة

## ① خطئي: أضفت الزرّ في واجهة واحدة من ثلاث

لتسجيل هدف في المنصة **ثلاث واجهات مستقلّة**، ولكلٍّ منها زرّ «هدف عكسي»
خاصّ بها:

| الواجهة | الدالّة |
|---|---|
| منتقي الهدّاف | `_spOwnGoal` |
| النتيجة السريعة | `qrCommitOwnGoal` |
| صفحة البثّ المباشر | `lpOwnGoal` |

أضفت «هدف بلا اسم» في الأولى فقط — فبدا مفقوداً في النتيجة السريعة والبثّ،
وهو ما رصدته بالضبط. أُضيف الآن في **الثلاث**، كلٌّ بكاتبه الموازي
(`qrCommitNoName` و`lpNoNameGoal`)، بنفس القواعد:

- نوع الحدث `goal` لا نوع جديد — كل حسابات النتائج تقرأ `goal` و`own` فقط.
- اسم اللاعب فارغ وعليه علامة `anon` — فلا يدخل جدول الهدّافين.
- يُحتسب للفريق وتظهر الكرة في المجريات عند الإدارة والجمهور.
- في البثّ يأخذ الدقيقة والشوط الحاليَّين من حالة المباراة الحيّة.

## ② صفحة الجمهور لا تفتح — `Invalid or unexpected token`

فحصت الملفين المذكورين بايتاً بايتاً:

- **لا BOM · لا محارف NUL · ترميز UTF-8 سليم**
- و**كلاهما يُحلَّل بلا خطأ**: `viewer.js` كوحدة ES، و`viewport-lock.js`
  كسكربت عادي.

فالملفان سليمان — والمتصفح **لا يستلمهما**. الخطأ عند `1:1` في ملف يبدأ
بتعليق `//` لا يقع من الشيفرة؛ يقع حين يصل المتصفح شيءٌ آخر: صفحة خطأ
HTML تبدأ بـ`<!DOCTYPE html>` — فأول حرف `<` هو «الرمز غير المتوقّع».

**السبب في الخادم أو الرفع، لا في الشيفرة:** ملف لم يُرفع، أو مسار خاطئ،
أو Service Worker قديم يخدم صفحة مكان ملف.

### كيف تتأكّد في نصف دقيقة

1. افتح رابط الملف مباشرةً في المتصفح:
   `https://موقعك/viewer.js` — إن ظهرت صفحة خطأ أو صفحة موقعك بدل شيفرة،
   فالملف غير مرفوع أو مساره خاطئ.
2. أو `F12 ← Network ← viewer.js` وانظر عمود **Type**: إن كان `document`
   أو `html` بدل `script` فهذه هي المشكلة عينها.

### وحُصِّن الـService Worker

أُضيف حارس يمنع تكرار هذا الالتباس مستقبلاً: أي استجابة لطلب ملف JS تصل
بنوع `text/html` أو بحالة غير 200 — **لا تُخزَّن ولا تُسلَّم** كأنها شيفرة،
ويُسجَّل تحذير واضح في الـConsole باسم الملف. فبدل خطأ مضلّل يشير إلى ملف
سليم، تعرف أن المشكلة في وصول الملف نفسه.

## الاختبارات

الواجهات الثلاث (زرّ ودالّة لكلٍّ) · النوع والعلامة والاسم الفارغ في
الكتّاب الثلاثة · مقابلها من الهدف العكسي · حارس الـSW بأربع حالات
(شيفرة سليمة تُقبل · صفحة خطأ تُرفض · HTML بحالة 200 يُرفض · مستند HTML
يُقبل كما هو) · وإعادة تشغيل سلاسل الاختبار — صفر أخطاء.

## بعد الرفع

`batolat-v295`. **تأكّد أن كل الملفات رُفعت** — خصوصاً `viewer.js` و
`viewport-lock.js` و`back-nav.js` — ثم امسح بيانات الموقع مرة واحدة.
