# 📷 صور اللاعبين — دليل التفعيل

## لماذا هذا الحل؟
صور اللاعبين تُرفع إلى **Firebase Storage** (حصته ٥ جيجا منفصلة تماماً عن حصة قاعدة
البيانات "الجيجا" التي كنت تعاني منها). يُخزَّن في قاعدة البيانات **رابط الصورة فقط**
(نص قصير) — فلا تأكل صور اللاعبين شيئاً يُذكر من حصة Firestore.

## خطوة واحدة مطلوبة منك (مرة واحدة فقط)
لكي يعمل الرفع، فعّل قواعد أمان التخزين:
1. افتح **Firebase Console** → مشروعك (footblle2).
2. من القائمة: **Storage** → تبويب **Rules**.
3. الصق محتوى ملف `storage.rules` المرفق، ثم اضغط **Publish**.

(إن لم تكن خدمة Storage مفعّلة بعد، اضغط **Get Started** في صفحة Storage أولاً.)

## كيف تضيف صورة لاعب
1. الإدارة → الفرق → افتح كشف الفريق.
2. بجانب كل لاعب زر **📷** — اضغطه واختر الصورة.
3. تُضغط الصورة تلقائياً وتُرفع، ثم تظهر مكان رقم القميص.

## أين تظهر للجمهور
- مكان رقم اللاعب في الكشف.
- في نافذة إحصائيات اللاعب (الصورة أعلى النافذة).
- اللاعبون بلا صورة يظهرون برقمهم كالمعتاد — لا شيء يتغيّر لهم.

## ملاحظات
- الحجم الأقصى للصورة المرفوعة: 8MB (تُضغط تلقائياً لحجم صغير قبل الرفع).
- لا شيء من نظامك القديم تغيّر: اللاعبون والفرق والمباريات تعمل كما هي تماماً.
- إن لم تُفعّل قواعد Storage، سيظهر خطأ واضح عند الرفع فقط — دون تعطيل أي ميزة أخرى.
