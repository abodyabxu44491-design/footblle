# v294 — «هدف بلا اسم» + إصلاح عدّ مباريات اللاعب

## ① ⚽ هدف بلا اسم — زرّ ثابت

المنظّم لا يعرف المسجِّل دائماً: كرة عشوائية، ازدحام أمام المرمى، بثّ
متأخّر. وكان أمامه خياران كلاهما خطأ:

- يكتب اسماً يظنّه → يُفسد جدول الهدّافين بهدف لم يسجّله صاحبه.
- أو يتخطّى الهدف → تصير **النتيجة نفسها خاطئة**.

**الآن زرّ ثابت** بجانب «هدف عكسي» في منتقي الهدّاف — في **الإدخال السريع
وصفحة البثّ** معاً. يُحتسب الهدف للفريق، وتظهر الكرة في مجريات المباراة عند
الإدارة والجمهور، **بلا نسبة لأحد**.

**قرار تقني مقصود:** نوع الحدث `goal` لا نوع جديد. كل ما يحسب النتائج في
المنصة وصفحة الجمهور يقرأ `goal` و`own` فقط — فإحداث نوع ثالث كان سيُسقط
الهدف من كل تلك الحسابات بصمت. الفارق أن `player` فارغ وعليه علامة `anon`،
وجداول الهدّافين تتخطّى الأحداث بلا اسم أصلاً (١٨ حارساً في الكود) فلا يدخل
أحد الجدول بغير حقّ.

ويحترم **عدّاد التكرار**: تختار «٣» فتُسجَّل ثلاثة أهداف بلا اسم دفعة واحدة.
والنتيجة تُعاد حسابها من الأحداث لا بزيادة عمياء.

## ② 🔴 عدّ مباريات اللاعب كان ناقصاً

مطابقة اللاعب بالتشكيلة كانت:

```js
player.playerId ? (pl.id && pl.id === player.playerId) : nameMatches(...)
```

تشترط المعرّف متى وُجد لدى اللاعب **بلا رجوع إلى الاسم**. ومدخل التشكيلة
قد يُحفظ بلا معرّف — حين يُكتب الاسم يدوياً في المحرّر بدل اختياره من
الكشف، وهو الشائع. فتفشل المطابقة وتضيع المشاركة، **فيظهر اللاعب بمباريات
أقلّ مما لعب فعلاً** — وهو ما لاحظته.

**القاعدة الصحيحة:** قارن بالمعرّف متى توفّر **عند الطرفين معاً**، وإلا
قارن بالاسم. وهي القاعدة المطبَّقة أصلاً في مطابقة أحداث التبديل — فتوحيدها
يمنع اختلاف النتيجة بين الحسابين.

**محاكاة على لاعب لعب ٣ مباريات** (واحدة من الكشف، وواحدة بالاسم يدوياً،
وواحدة بلا حقل معرّف):

| | المحسوب |
|---|---|
| القاعدة القديمة | **١** — تُسقط مباراتين |
| القاعدة الجديدة | **٣** ✓ |

ويسري الإصلاح على كل ما يُبنى على المشاركة: عدد المباريات، ومعدّل الأهداف
لكل مباراة، وقائمة مباريات اللاعب في صفحة الجمهور.

## الاختبارات

الزرّ وظهوره ومساراه (البثّ والإدخال السريع) · النوع `goal` والاسم الفارغ ·
عدّاد التكرار · إعادة حساب النتيجة من الأحداث · حرّاس جدول الهدّافين ·
قاعدة المطابقة الجديدة ومحاكاة العدّ · وإعادة تشغيل الثلاث عشرة سلسلة —
صفر أخطاء.

## بعد الرفع

`batolat-v294` وكل المراجع `?v=294`. امسح بيانات الموقع مرة واحدة.
